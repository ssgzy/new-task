# 诊断更新：基于 debug smoke 数据的修正分析

> 写于 2026-04-04，基于 `outputs/debug/input_interface_diagnosis/*.json` 和 `outputs/debug/input_smoke/*/smoke_compare.json` 的实际输出。

---

## 初始建议的修正

我在 2026-04-02 的建议中说"只要补 `apply_chat_template()` 就能修 Lion / Orca"，这个结论有误。实验已经证明：

- 三个模型（Lion / Orca / MiniLLM）本地 tokenizer **确实没有** `chat_template` 字段
- 因此 `auto` 模式只能 fallback 到 plain prompt，结果和之前完全一样
- "chat template 缺失是可以 apply_chat_template() 修复的"这一步在本地事实下不成立

但"chat template 未应用"这个诊断方向**本身没有错**，只是解法需要更换一层：问题不是代码没调 `apply_chat_template()`，而是**本地 tokenizer snapshot 根本不带 chat_template，需要手动硬编码各模型的 instruction format**。

---

## 四个模型的真实问题（基于 smoke 输出）

### 1. Orca-2-7B：缺失 ChatML 格式，模型在做文档补全

**smoke 输出**：
```
5 . 9 ▁m , ▁s 2 0 1 4 ▁m , ▁s 2 0 1 4 ▁m , ▁s 2 0 1 4 ...
```

这是在逐字重复 table 里的 `2014` 单元格。这是**基础语言模型的文档续写行为**，不是 instruction-following 行为。

**原因**：Orca-2-7B 是在 ChatML 格式下对齐的，其格式为：
```
<|im_start|>system
You are Orca, an AI language model...
<|im_end|>
<|im_start|>user
{question}
<|im_end|>
<|im_start|>assistant
```
不用这个格式，模型看到的就是一段原始文本，它的最优补全策略是"继续这个文档"，也就是重复表格内容。这不是 repetition_penalty 能解决的问题。

### 2. Lion-7B：缺失 Vicuna 格式，从首个 token 开始输出乱码

**smoke 输出**：
```
xcfoo accom somebody theoret!!! incor behaviour http « behaviour http « ...
```

第一个 token 就是无意义字符。这比 Orca 更严重——Orca 至少在做有逻辑的文档续写，Lion 连这个都没有。

**原因**：Lion-7B 是基于 Vicuna 格式蒸馏的，标准格式是：
```
A chat between a curious user and an artificial intelligence assistant.
The assistant gives helpful, detailed, and polite answers to the user's questions.

USER: {instruction}
ASSISTANT:
```
没有这个 header 和 `USER:`/`ASSISTANT:` 分隔符，模型的 instruction-following 路径完全无法激活。"xcfoo accom" 这类输出说明模型在没有正确触发点的情况下处于完全退化状态。

### 3. Zephyr-7B-beta：chat_template 生效，但导致格式与 parser v1 冲突

**smoke 对比**：
- plain 模式 → `Answer: 112.24%\n\nExplanation: To calculate...` → `valid_parse=True`
- chat_template 模式 → `Answer: To calculate the percentage increase, we first find...` → `valid_parse=False`

**原因**：chat_template 改变了模型的 generation style。在 chat 模式下，Zephyr 把 `Answer:` 理解成段落标题，在后面跟了解释文本。在 plain 模式下，Zephyr 更倾向于直接输出数字。

**结论**：对 Zephyr，**plain 模式 + 当前 parser v1 反而更兼容**，不需要强制启用 chat_template。这是一个"模板模式改变了输出风格，使其与 parser 冲突"的案例，不是"模板能提升质量"的案例。

### 4. MiniLLM-Llama-7B：模型输出数学表达式，不是纯数值

**smoke 输出**：
```
Answer: 100 * (1 - 0.0324 / 5.99) = 100 * (1 - 0.0324 / 5.99) = ...
```

模型在 `Answer:` 后面写了计算式而不是数值结果，且进入了重复循环。

**原因**：MiniLLM-Llama-7B 是通过最小化逆 KL 散度做 distillation 的基础模型，**不是 instruction-tuned 模型**。它对 "Do not explain. Output exactly one final line." 这类 instruction 的遵从能力本就不强。用 plain prompt 给一个 base distilled model 做 benchmark，它会把 `Answer:` 理解为"开始计算"的信号，然后展开算式。

---

## 可操作的修复路径（按优先级）

### 路径 A：为 Orca-2-7B 和 Lion-7B 硬编码已知 instruction format（最高优先级）

这不是"为个别模型改 parser"，而是"用正确的 prompt 格式驱动模型"——两者不同，不违反协议中"不为单个模型编写特殊 parser"的约定。

在 `finqa_protocol_v1.py` 中增加一个 `model_instruction_format` 注册表：

```python
# 已知 instruction format，用于无 chat_template 的 instruction-tuned 模型
KNOWN_INSTRUCTION_FORMATS = {
    "Orca-2-7B": "chatml",   # <|im_start|>system ... <|im_end|> ...
    "Lion-7B":   "vicuna",   # A chat between... USER: ... ASSISTANT:
}

CHATML_TEMPLATE = (
    "<|im_start|>system\n{system}\n<|im_end|>\n"
    "<|im_start|>user\n{user}\n<|im_end|>\n"
    "<|im_start|>assistant\n"
)

VICUNA_TEMPLATE = (
    "A chat between a curious user and an artificial intelligence assistant. "
    "The assistant gives helpful, detailed, and polite answers to the user's questions.\n\n"
    "USER: {system}\n\n{user}\nASSISTANT:"
)
```

渲染函数修改为：先查 `tokenizer.chat_template`，若没有则查 `KNOWN_INSTRUCTION_FORMATS`，都没有才 fallback 到 plain。

**验证**：在 `outputs/debug/` 下跑 1 条 smoke，Orca 应不再重复 "2014"，Lion 应不再输出 "xcfoo"。

### 路径 B：修复 Lion-7B 和 MiniLLM 的 pad_token_id 配置（次优先级）

当前 runner 显式传 `pad_token_id=tokenizer.pad_token_id`，所以这个不一致暂时不影响 generation 结果。但为了消除隐患，建议在 model/tokenizer 加载后显式同步：

```python
if model.config.pad_token_id is None or model.config.pad_token_id == 0:
    model.config.pad_token_id = tokenizer.pad_token_id
```

### 路径 C：确认 MiniLLM 的定位（需要决策）

MiniLLM-Llama-7B 是 base distilled model，不是 instruction-tuned model。两个选项：

1. **用 completion-style prompt 驱动**：去掉 System/User 框架，直接以 `{context}\nQ: {question}\nA:` 格式触发。这与其他模型的 prompt 格式不统一，但更符合 base model 的工作方式。
2. **降低对 MiniLLM 的期望，接受它作为"base distilled model baseline"**：在结果表中单独标注它不是 instruction-tuned model，解释格式合规率较低的原因。

选项 2 更简单，也更符合 proposal 的"benchmark-first"逻辑：如果 MiniLLM 在 direct-answer 协议下表现差，这本身就是一个有价值的发现，说明 instruction-following 对 benchmark 合规性至关重要。

### 路径 D：Zephyr 保持 plain 模式（不需要修改）

Zephyr 在 plain 模式下 `valid_parse=True`，说明它可以正常跟随 direct-answer 指令。chat_template 反而导致了格式退化。结论：对 Zephyr，`render_mode=plain` 是当前协议下的正确配置，不需要改动。

---

## 推荐执行顺序

```
Step 1（今天）：
- 在 finqa_protocol_v1.py 中加入 KNOWN_INSTRUCTION_FORMATS 注册表
- 为 Orca-2-7B 实现 ChatML wrapper，为 Lion-7B 实现 Vicuna wrapper
- 在 run_finqa_local_benchmark_v1.py 中修改 render_prompt 分流逻辑
- 只在 outputs/debug/ 下对 Lion 和 Orca 各跑 1 条 smoke 验证

Step 2（smoke 通过后）：
- 确认 Lion / Orca smoke 不再出现 repetition_collapse
- 修复 pad_token_id 同步（路径 B）
- 确认 MiniLLM 的定位（路径 C 决策）
- 对 4 个主表模型重新跑 val_calib50（50 条），得到有意义的 calibration 数字

Step 3（calibration 完成后）：
- 重新做长度冻结决策
- 进入 val_screen200
```

---

## 一句话核心结论

**Lion 和 Orca 的问题不是 `apply_chat_template()` 没有被调用，而是本地 tokenizer snapshot 根本不带 `chat_template` 字段——解法是在 protocol 层显式注册并硬编码这两个模型的已知 instruction format（ChatML / Vicuna），而不是修改 parser。Zephyr 已经在 plain 模式下工作正常，不需要修改。**

---

*本文档由 Claude (claude-sonnet-4-6) 于 2026-04-04 基于 debug smoke 数据更新，修正了 2026-04-02 初始分析中对 apply_chat_template() 可行性的过度乐观估计。*
