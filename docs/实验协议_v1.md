# 实验协议_v1

## 状态
- 当前状态：已代码冻结，等待模型下载完成后执行
- 相关文档：[[项目总览]]、[[数据重拉与字段核对]]、[[长度校准报告]]、[[准入筛选总结]]、[[异常与决策记录]]

## 协议文件
- 协议代码：`scripts/finqa_protocol_v1.py`
- 导出目录：`outputs/protocol_v1/`
- 导出内容：
  - `protocol_v1.json`
  - `prompt_system_v1.txt`
  - `prompt_user_v1.txt`
  - `decode_config_v1.json`

## Prompt v1
### System
```text
You are a precise financial question answering assistant.
Use only the provided table and text.
Do not show your reasoning.
Do not explain.
Output exactly one final line in this format:
Answer: <numeric value>
```

### User
```text
### Context
{context}

### Question
{question}
```

## Parser v1
- 统一只认最后答案行。
- 不为单个模型编写特殊 parser。
- 计划记录字段：
  - `format_ok`
  - `valid_parse`
  - `pred_value`
  - `pred_unit_type`
  - `answer_line_raw`
  - `multiple_answer_lines`
  - `truncated_suspect`
  - `parse_error_reason`
- `truncated_suspect` 冻结规则：
  - 文本末尾启发式命中
  - 或者 `new_tokens == max_new_tokens` 且缺失 `Answer:` 行

## Gold 口径
- `raw_answer` 保留原始答案字段值，优先取 `qa.answer`，缺失时回退到 `qa.exe_ans`，并在 `source_answer_field_name` 中记录来源。
- `gold_numeric` 统一标准化规则：
  - 去掉 `$`、`,`、`%`
  - 处理括号负号
  - `yes -> 1.0`
  - `no -> 0.0`
- `qa.answer` 与 `qa.exe_ans` 不可视为同义字段。validation `623/883` 条存在数值差异，主要来自百分比缩放或四舍五入差异，因此评测 gold 以 `raw_answer` 标准化结果为准。

## Decode Config
- 今晚长度校准先从 `max_new_tokens=256` 起步。
- 明天再根据校准结果决定是否降到 `192`。
- 候选档位：`128 / 192 / 256`
- 当前冻结草案：
  - `temperature = 0.0`
  - `top_p = 1.0`
  - `do_sample = false`
  - `max_new_tokens = 256`（今晚默认起步）

## 2026-04-07 冻结决策
- 本轮 `val_screen200` 先按单一冻结档位执行：
  - `max_new_tokens = 256`
- 本轮 `screen200` 准入门槛冻结为：
  - `runtime_success >= 0.95`
  - `format_ok >= 0.80`
  - `valid_parse >= 0.60`
  - `truncation_without_answer_rate <= 0.10`
- 这一步只做 `val_screen200`，不触发 `validation883`：
  - 必须使用 `--screen-only`
- 本轮新结果只写 `provisional`：
  - 必须使用 `--provisional`
- 本轮 6 模型筛选目录冻结到：
  - `outputs/provisional/screen200_6model_20260407/`
- 本轮 6 模型列表：
  - `Orca-2-7B`
  - `Zephyr-7B-beta`
  - `Qwen2.5-7B-Instruct`
  - `Mistral-7B-Instruct-v0.3`
  - `Yi-1.5-6B-Chat`
  - `ChatGLM3-6B`
- `ChatGLM3-6B` 继续沿用已验证可用的手动 generation patch；这一步的目标是确认它在 `200` 条规模下仍稳定，而不是改 parser 或改任务语义。

## Context 构造
- 由 `scripts/finqa_protocol_v1.py` 固定实现。
- 拼接顺序：
  - `### Pre Text`
  - `### Table`
  - `### Post Text`
- `table` 逐行转为 `cell1 | cell2 | ...` 形式。
- `context_char_len` 以该完整 context 串的字符数计算。

## 评测约束
- 禁止在本轮中使用 `test1147` 做筛选、调 prompt、调 parser、调 token 长度或 student selection。
- 长度校准使用 `val_calib50`。
- 准入筛选使用 `val_screen200`。
- 正式验证使用 validation `883`。
- 当前冻结状态下，`val_screen200` 必须逐模型执行并逐个回写结果到 `provisional` 汇总表。

## 补充评分说明（不改变主协议）
- `strict scoring` 仍是主线。
- `parser v1` 不变：
  - 只认最后一条 `Answer:` 行
  - `Answer:` 后必须是合法纯数值
- 新增 `relaxed scoring` 只作为补充指标，不替代 strict 结果。
- 当前主准确率指标仍定义为：
  - `Strict TM`
- 新增补充指标：
  - `relaxed_em`
  - `relaxed_tm`
  - `relaxed_gap_tm`
- 解释口径：
  - `Relaxed TM` = 推理能力上界
  - `relaxed_gap_tm` = 格式问题吃掉的 TM 准确率

## 待补充
- 运行时统计口径
- 输出文件路径与命名规范
