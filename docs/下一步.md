# 下一步

## 立即执行
1. 基于已经完成的 DeepSeek / OpenR1 官方 wrapper 初查，先决定是否值得为两者各设计一条“官方 messages 序列化”provisional 对照 smoke；如果做，必须仍然不改 parser、不改 direct-answer 任务、不覆盖 canonical。
2. Orca-2-7B：先保留为“可比较但有残余风险”的正式候选；如果下一轮只做一个 provisional 最小实验，优先补测 `max_new_tokens=384`，依据见 [[过程总结_orca_下一步决策]]。
3. Xwin-LM-7B：当前 Vicuna 路径 smoke 已退化为连续 `<unk>`，不要重复跑同一路径；若继续，只做 tokenizer/special-token 专项核查。
4. Lion-7B：继续单列为历史锚点 / 协议失败案例，不再拉回正式主表。
5. MiniLLM-Llama-7B：继续保持 `base_distilled baseline` 定位，本轮不再调 wrapper。
6. LaMini-LLaMA-7B：继续等待一个可公开访问且可复现的 repo_id，本轮不引入社区镜像。

## 当前不再重复做什么
- 不再重跑 `Xwin-LM-7B` 当前 Vicuna 路径下同样的 1 条 smoke。
- 不再继续把 `Lion-7B` 拉回正式主表。
- 不再继续调 `MiniLLM-Llama-7B` wrapper。
- 不恢复 canonical `val_calib50`。
- 不恢复 canonical `val_screen200`。
- 不改 parser。
- 不把 benchmark 主协议改成 CoT。
- 不再假设 DeepSeek/OpenR1 当前 plain/chat_template 输出失败只是“随机没跑好”；DeepSeek 已确认官方模板会显式引导 `<think>`，OpenR1 已确认官方入口更接近 `pipeline(messages)` 而不是手写 plain 拼接。

## 随后执行
1. 如果 Lion 的有效输入协议问题也被修复，再评估是否重跑全主表 canonical `val_calib50`。
2. 如果 Lion 继续在官方 Alpaca-style 下失败，则优先把 Lion 单列为“输入协议未通过”的历史 anchor 风险项，而不是先改 CoT 或 screening 门槛。
3. 如果 Orca 在 `384` provisional 补测后仍无法压到 2% 截断安全口径，则继续保持“只跑 provisional，不恢复 canonical”。
4. 在新增主表候选池补到不少于 4 个“已进入有效输入协议”的模型之前，不启动正式主表 `val_screen200`。

## 关联文档
- [[当前状态]]
- [[任务笔记 - FinQA 基准重建]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_主表扩展与新增模型筛查]]
- [[过程总结_协议可用性筛查]]
- [[过程总结_orca_下一步决策]]
- [[过程总结_新增候选失败类型分类]]
- [[会话日志]]
- [[模型注册表]]
- [[异常与决策记录]]
- [[组员运行说明]]
