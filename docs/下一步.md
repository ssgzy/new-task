# 下一步

## 立即执行
1. 继续等待 fresh 6 模型 `val_calib50` 跑完，并只读取：
   - `outputs/provisional/calibration_runs_6model_20260407/`
   - `outputs/provisional/calibration_report.6model_mainstream_20260407.csv`
   已经不再适用；本轮 run 已结束，下一步应改为修复单档位证据不一致问题
2. 当前已确认：
   - `Orca-2-7B` 三档：`0.18 / 0.10 / 0.10`
   - `Zephyr-7B-beta` 三档：`0.32 / 0.14 / 0.06`
   - `Qwen2.5-7B-Instruct` 三档：`0.00 / 0.00 / 0.00`
   - `Mistral-7B-Instruct-v0.3` 三档：`0.00 / 0.00 / 0.00`
   - `Yi-1.5-6B-Chat` 三档：`0.02 / 0.02 / 0.02`
   - `ChatGLM3-6B` 的 `/192` 与 `/256`：`0.04 / 0.04`
3. 当前最优先动作：
   - 只重跑 `ChatGLM3-6B / 128`
   - 对齐 `predictions.jsonl` 行数和 `summary.json.num_examples`
   - 然后重建 `calibration_report.6model_mainstream_20260407.csv`
4. 在 `ChatGLM3-6B / 128` 修复前，不冻结统一长度；本轮 aggregate csv 仅记为 `provisional_suspect`。
5. Orca-2-7B：如果修完 `ChatGLM3-6B / 128` 后仍无法给出“全体模型共同安全”的统一档位，再回到 [[过程总结_orca_下一步决策]] 讨论是否补 `384`。
6. `Llama-3.1-8B-Instruct` 与 `Gemma-7B-IT` 当前仍是“官方 gated repo 未获访问权限”，不要把它们误算为 smoke 协议失败。
7. 基于已经完成的 DeepSeek / OpenR1 官方 wrapper 初查，等这一轮 calibration 结束后再决定是否值得做各自的官方 messages 序列化对照 smoke；本轮先不插入新实验。
8. Xwin-LM-7B：当前 Vicuna 路径 smoke 已退化为连续 `<unk>`，不要重复跑同一路径；若继续，只做 tokenizer/special-token 专项核查。
9. Lion-7B：继续单列为历史锚点 / 协议失败案例，不再拉回正式主表。
10. MiniLLM-Llama-7B：继续保持 `base_distilled baseline` 定位，本轮不再调 wrapper。
11. LaMini-LLaMA-7B：继续等待一个可公开访问且可复现的 repo_id，本轮不引入社区镜像。

## 当前不再重复做什么
- 不再重跑 `Xwin-LM-7B` 当前 Vicuna 路径下同样的 1 条 smoke。
- 不再继续把 `Lion-7B` 拉回正式主表。
- 不再继续调 `MiniLLM-Llama-7B` wrapper。
- 不恢复 canonical `val_calib50`。
- 不恢复 canonical `val_screen200`。
- 不改 parser。
- 不把 benchmark 主协议改成 CoT。
- 不再假设 DeepSeek/OpenR1 当前 plain/chat_template 输出失败只是“随机没跑好”；DeepSeek 已确认官方模板会显式引导 `<think>`，OpenR1 已确认官方入口更接近 `pipeline(messages)` 而不是手写 plain 拼接。

## 随后执行
1. 如果 Lion 的有效输入协议问题也被修复，再评估是否重跑全主表 canonical `val_calib50`。
2. 如果 Lion 继续在官方 Alpaca-style 下失败，则优先把 Lion 单列为“输入协议未通过”的历史 anchor 风险项，而不是先改 CoT 或 screening 门槛。
3. 如果 Orca 在 `384` provisional 补测后仍无法压到 2% 截断安全口径，则继续保持“只跑 provisional，不恢复 canonical”。
4. 在新增主表候选池补到不少于 4 个“已进入有效输入协议”的模型之前，不启动正式主表 `val_screen200`。

## 关联文档
- [[当前状态]]
- [[任务笔记 - FinQA 基准重建]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_主表扩展与新增模型筛查]]
- [[过程总结_协议可用性筛查]]
- [[过程总结_orca_下一步决策]]
- [[过程总结_新增候选失败类型分类]]
- [[会话日志]]
- [[模型注册表]]
- [[异常与决策记录]]
- [[组员运行说明]]

## 2026-04-07 最新更新
1. fresh 6 模型 provisional calibration 已完成，且 `ChatGLM3-6B / 128` 的证据不一致问题已修复。
2. 当前最终结果：
   - `Orca-2-7B = 0.18 / 0.10 / 0.10`
   - `Zephyr-7B-beta = 0.32 / 0.14 / 0.06`
   - `Qwen2.5-7B-Instruct = 0.00 / 0.00 / 0.00`
   - `Mistral-7B-Instruct-v0.3 = 0.00 / 0.00 / 0.00`
   - `Yi-1.5-6B-Chat = 0.02 / 0.02 / 0.02`
   - `ChatGLM3-6B = 0.46 / 0.04 / 0.04`
3. 统一冻结口径仍是 `truncation_without_answer_rate <= 0.02`。
4. 结论：`128 / 192 / 256` 三档都不能让全部 6 模型同时进入安全范围。
5. 下一步最值得做的是二选一：
   - 扩到 `320 / 384`
   - 或接受“当前没有统一安全档”的结论，并按模型分层汇报

## 2026-04-07 最新更新：立即执行 val_screen200
1. 当前先不继续扩 `320 / 384`，先执行冻结后的 `val_screen200`。
2. 冻结规则：
   - `max_new_tokens = 256`
   - `runtime_success >= 0.95`
   - `format_ok >= 0.80`
   - `valid_parse >= 0.60`
   - `truncation_without_answer_rate <= 0.10`
3. 当前 run root：
   - `outputs/provisional/screen200_6model_20260407/`
4. 当前汇总表：
   - `outputs/provisional/screen200_6model_20260407/screen200_status_table.md`
5. 当前最直接下一步：
   - `Orca-2-7B` 已完成并通过
   - `Zephyr-7B-beta` 当前出现结果一致性异常
   - 先修 `Zephyr`，不继续下一个模型

## 2026-04-07 Zephyr 修复优先级
1. 归档当前 `Zephyr-7B-beta` 的可疑目录。
2. 单独重跑 `Zephyr-7B-beta / val_screen200 / 256`。
3. 确认：
   - `summary.json.num_examples`
   - `predictions.jsonl` 行数
   - 二者一致后再刷新 `screen200_status_table`

## 2026-04-07 Zephyr 修复后下一步
1. `Zephyr-7B-beta` 已完成修复并正式记为 `ok / no`。
2. 当前下一个模型：
   - `Mistral-7B-Instruct-v0.3`
3. 继续规则不变：
   - 单模型逐个跑
   - 每出现中间落盘样本就先写日志
   - 不启动下一个模型前，先把当前模型结果写入表

## 当前正在执行
1. `Yi-1.5-6B-Chat` 已完成并通过
2. 当前下一个模型：
   - `ChatGLM3-6B / val_screen200 / 256`
3. 当前等待：
   - `ChatGLM3-6B` 启动并写入第一批样本

## 2026-04-07 ChatGLM3 当前下一步
1. 继续等待 `ChatGLM3-6B / val_screen200 / 256` 生成 `summary.json`
2. 一旦生成：
   - 先检查 `summary.json.num_examples`
   - 再检查 `predictions.jsonl` 行数
   - 二者一致后再写正式结果
3. 在 `ChatGLM3` 结束前：
   - 不启动新的 `screen200` 模型

## 2026-04-07 screen200 完成后下一步
1. 当前 `6` 模型 `val_screen200` 已全部完成
2. 当前优先工作：
   - 基于现有 `screen200` 结果整理正式候选与未通过模型
   - 更新 `[[准入筛选总结]]`
   - 明确是否继续进入下一阶段
3. 当前不再重复：
   - 不重跑已完成且一致性正常的 `screen200`
   - 不覆盖现有 `provisional` 结果

## 2026-04-07 整理后当前下一步
1. `screen200` 汇总已经完成
2. 当前最直接下一步：
   - 以 `Orca-2-7B`、`Mistral-7B-Instruct-v0.3`、`Qwen2.5-7B-Instruct`、`Yi-1.5-6B-Chat` 作为通过名单，决定是否进入下一阶段
3. 如需引用当前结果：
   - 优先使用 `outputs/provisional/screen200_6model_20260407/screen200_final_summary.md`

## 2026-04-07 报告整理后下一步
1. 当前 strict / relaxed 双评分逻辑已接入，但还没有重跑新的 `validation883`
2. 当前最合理下一步：
   - 决定是否让组员按 `4` 个正式主表模型分工运行 `validation883`
3. 当前建议使用：
   - `[[组员运行说明_validation883]]`
   - `[[当前主表建议]]`
   - `[[评分逻辑与报告写作指南]]`

## 2026-04-07 当前默认入口说明
1. 当前组员默认入口已经是：
   - `validation883`
2. 当前推荐命令：
   - `bash scripts/run_assigned_model_v1.sh --label "<模型名>" --download-model`
3. 如需回退旧 `screen200` 流程：
   - 显式加 `--legacy-screen-only`

## 2026-04-07 validation883 当前执行顺序
1. 当前总名单：
   - `Orca-2-7B`
   - `Mistral-7B-Instruct-v0.3`
   - `Qwen2.5-7B-Instruct`
   - `Yi-1.5-6B-Chat`
   - `ChatGLM3-6B`
2. 当前正在跑：
   - `Qwen2.5-7B-Instruct`
3. 当前计划：
   - 今晚先完成 `Qwen2.5-7B-Instruct`
   - 之后再把剩余 `4` 个模型分配给组员
4. 当前进度：
   - `Qwen2.5-7B-Instruct` 已完成
   - `Mistral-7B-Instruct-v0.3` 已完成
5. 当前最直接下一步：
   - 选择下一个 `validation883` 模型继续跑
6. 当前查看入口：
   - `outputs/provisional/validation883_assigned/validation883_status_table.md`
