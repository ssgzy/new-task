# 当前状态

## 基本信息
- 更新时间：2026-04-07 HKT
- 当前阶段：已切入“主流 instruct/chat 模型 1 条 smoke 预筛”批次；旧的协议可用性筛查结论仍有效，当前已进入有效输入协议的模型仍只有 `Orca-2-7B` 与 `Zephyr-7B-beta`
- 当前目标：对 `Mistral/Qwen2.5/Llama-3.1/Gemma/Yi/ChatGLM3` 做官方仓库下载检查与 1 条 smoke 预筛，并在不恢复 canonical 主线的前提下，从 smoke 通过者中收束最终 6 模型候选
- 工作目录：`/Users/sam/Documents/Documents - sam的MacBook Pro/论文📑/Distillation/new task`
- 环境：`conda run -n Lion`

## 已完成
- 确认 `new task` 目录存在且可用。
- 确认 `Lion` conda 环境存在。
- 初始化基础目录：`docs/`、`logs/`、`outputs/`、`data/`、`scripts/`。
- 建立首批项目文档与日志入口。
- 从官方 GitHub 成功重拉 `train/dev/test` 三个原始 split。
- 生成标准化数据：
  - `data/processed/finqa/train.standardized.jsonl`
  - `data/processed/finqa/validation.standardized.jsonl`
  - `data/processed/finqa/test.standardized.jsonl`
- 生成三个固定 manifest：
  - `data/manifests/val_calib50.jsonl`
  - `data/manifests/val_screen200.jsonl`
  - `data/manifests/validation883.jsonl`
- 输出字段核对与 manifest 元数据：
  - `outputs/metadata/finqa_split_summary.csv`
  - `outputs/metadata/finqa_manifest_index.csv`
- 冻结了协议代码与机器可读 bundle：
  - `scripts/finqa_protocol_v1.py`
  - `outputs/protocol_v1/protocol_v1.json`
  - `outputs/protocol_v1/prompt_system_v1.txt`
  - `outputs/protocol_v1/prompt_user_v1.txt`
  - `outputs/protocol_v1/decode_config_v1.json`
- 实现了统一 runner 和两个汇总脚本：
  - `scripts/run_finqa_local_benchmark_v1.py`
  - `scripts/run_length_calibration_v1.py`
  - `scripts/run_qualification_v1.py`
- 新增单模型运行与环境固化资产：
  - `scripts/run_assigned_model_v1.sh`
  - `requirements-benchmark.txt`
- 三个入口脚本现已支持 `--labels`，可按精确模型名单跑：
  - `scripts/ensure_candidate_models.py`
  - `scripts/run_length_calibration_v1.py`
  - `scripts/run_qualification_v1.py`
- `scripts/run_qualification_v1.py` 现已支持 `--screen-only`，可只执行 `val_screen200`
- 新增组员执行说明：[[组员运行说明]]
- 新增了模型状态入口：[[模型注册表]]
- 已用 `Lion-7B` 跑通 1 条 smoke，确认 runner 能产出 summary / predictions 文件。
- 已将 `Lion` 环境固定为 Orca 可加载的版本组合：
  - `protobuf == 3.20.3`
  - `tiktoken == 0.12.0`
- 已验证 `Orca-2-7B` tokenizer 可以正常初始化。
- 已用 `Orca-2-7B` 跑通 1 条 smoke，确认完整 runner 链路恢复：
  - `outputs/smoke/orca_smoke_predictions_v1.jsonl`
  - `outputs/smoke/orca_smoke_summary_v1.json`
- 已恢复 canonical `outputs/metadata/model_registry.json/csv`。
- 已将被污染的旧 `outputs/calibration_report.csv` 归档到：
  - `outputs/provisional/archive/calibration_report.pre_restore.invalid.csv`
- canonical `outputs/calibration_report.csv` 已生成。
- 新增输入接口专项诊断文档：[[输入接口诊断]]
- 已实现并运行旧 v1 输入接口诊断与 1 条 smoke 对照，旧输出已归档：
  - `scripts/debug_input_interface_v1.py`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_interface_diagnosis/*.json`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_interface_diagnosis/*.txt`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_smoke/*/smoke_compare.json`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_smoke/*/smoke_compare.txt`
- 新增 wrapper registry v2 与 special-token alignment 修复逻辑：
  - `scripts/finqa_protocol_v1.py`
  - `scripts/run_finqa_local_benchmark_v1.py`
  - `scripts/debug_wrapper_registry_v2.py`
- 已生成 wrapper registry v2 provisional 输出：
  - `outputs/debug/wrapper_registry/registry_index.json`
  - `outputs/debug/wrapper_registry/*.json`
  - `outputs/debug/wrapper_registry/*.txt`
  - `outputs/debug/input_smoke_v2/*/smoke_v2.json`
  - `outputs/debug/input_smoke_v2/*/smoke_v2.txt`
  - `outputs/debug/input_smoke_v2/summary.json`
- 新增过程总结文档：[[过程总结_输入包装修复]]
- 新增协议筛查收束文档：
  - [[过程总结_协议可用性筛查]]
  - [[过程总结_orca_下一步决策]]
  - [[过程总结_新增候选失败类型分类]]
  - `outputs/debug/protocol_screening_registry/current_candidate_status_table.json`
  - `outputs/debug/protocol_screening_registry/current_candidate_status_table.md`
- 已复核官方输入格式来源：
  - Lion 官方 `train.py` 使用 Alpaca-style `### Instruction / ### Response`
  - Orca 官方模型卡要求 ChatML 且 `use_fast=False`
  - MiniLLM 官方资料没有给单一固定 inference wrapper
- 已完成 v3 最小修复与 2 模型 1 条 debug smoke：
  - `scripts/debug_wrapper_registry_v3.py`
  - `outputs/debug/input_smoke_v3/Lion-7B/smoke_v3.json`
  - `outputs/debug/input_smoke_v3/Orca-2-7B/smoke_v3.json`
  - `outputs/debug/input_smoke_v3/summary.json`
- 已归档 v3a 旧偏差输出：
  - `outputs/debug/archive/debug_failed_attempts/input_smoke_v3a_orca_autotokenizer_use_fast_false_not_effective_20260404/input_smoke_v3/`

## 进行中
- 已启动新增 6 模型的 provisional 预筛批次：
  - 过程文档：[[过程总结_主流Instruct预筛]]
  - 日期日志：[[2026-04-07_会话日志]]
- `hf auth whoami` 当前返回 `Not logged in`。
- 已通过实际官方下载探测确认：
  - `Llama-3.1-8B-Instruct`：`download_failed`，原因是官方 gated repo 需要登录并获得访问权限
  - `Gemma-7B-IT`：`download_failed`，原因是官方 gated repo 需要登录并获得访问权限
- 因此这两者当前先记为“官方仓库门控失败”，不计作 smoke 协议失败。
- bulk 下载仍在继续，当前非门控待 smoke 的新增模型是：
  - `Mistral-7B-Instruct-v0.3`
  - `Qwen2.5-7B-Instruct`
  - `Yi-1.5-6B-Chat`
  - `ChatGLM3-6B`
- 2026-04-07 00:55 HKT 复查：
  - bulk 下载主进程仍存活
  - `Mistral-7B-Instruct-v0.3` cache 已到约 `4.0G`
  - Mistral 的 4 个 `.incomplete` shard 仍在增长
  - `Qwen2.5-7B-Instruct`、`Yi-1.5-6B-Chat`、`ChatGLM3-6B` 当前尚未进入权重主体下载
- 当前还没有任何一个新增主流模型完成可运行落盘，因此还没有进入 smoke 阶段。
- 用户已明确要求改成“下载一个试一个”。
- 因此当前执行方式已从 bulk 下载切换为单模型串行：
  - 旧 bulk 下载进程已停止
  - 已保留全部已落盘 HF cache
  - 现在先续传 `Mistral-7B-Instruct-v0.3`，完成后立刻跑它的 1 条 smoke
- 最新复查：
  - `Mistral-7B-Instruct-v0.3` 已从约 `4.0G` 增长到约 `4.8G`
  - 4 个 shard 仍在持续增长，说明单模型续传有效
- `Mistral-7B-Instruct-v0.3` 已经完成下载与 1 条 smoke：
  - `effective_wrapper=chat_template`
  - `answer_appears=True`
  - `valid_parse=False`
  - `truncation_suspect=False`
  - `parse_error_reason=invalid_numeric_literal`
  - `raw_generation_text='Answer: 100 * (5.99 - 3.24) / 3.24 = 86.47%'`
- 当前结论：
  - `Mistral-7B-Instruct-v0.3` 还没有进入有效输入协议
  - 但它的失败类型已经收束为“答案行含算式，不是纯数值”，不是截断/无答案行/生成崩塌
- 当前正在下载的下一个单模型是：
  - `Qwen2.5-7B-Instruct`
- 主表 screening 继续暂停。
- 本轮只分析 `outputs/debug/` 下的 provisional smoke / diagnosis，不使用这些结果覆盖 canonical 主表。
- 已开始主表扩展筛查，但因当前网络切到热点，`Xwin-LM-7B` 下载已按用户指令暂停，partial cache 保留在 HF cache 中。
- 已新增 `scripts/ensure_candidate_models.py --local-only`，可只扫描本地 HF cache 生成 provisional registry，不再触发远程 `model_info()` / 下载请求。
- 已离线生成新增候选池 provisional registry：
  - `outputs/metadata/provisional/model_registry.groups-main-candidate-expansion.json`
  - `outputs/metadata/provisional/model_registry.groups-main-candidate-expansion.csv`
- 已完成新增 4 模型的 expansion debug / smoke 本地首轮输出：
  - `outputs/debug/wrapper_registry_expansion/*.json`
  - `outputs/debug/input_interface_diagnosis_expansion/*.json`
  - `outputs/debug/input_smoke_expansion/*/smoke_expansion.json`
  - `outputs/debug/input_smoke_expansion/summary.json`
- expansion smoke 首轮结论：
  - `Xwin-LM-7B`：`incomplete_local_snapshot`，因 hotspot 暂停下载导致本地 cache 仍有 `.incomplete` blob，本轮跳过 smoke
  - `LaMini-LLaMA-7B`：`pending_repo_id`，本轮跳过 smoke
  - `DeepSeek-R1-Distill-Qwen-7B -> chat_template`：`Answer=False / valid_parse=False / truncation_suspect=True`
  - `OpenR1-Distill-7B -> plain`：`Answer=True` 但 `answer_line_raw='Answer: <numeric value>'`，`valid_parse=False / parse_error_reason=invalid_numeric_literal`
- 因此本轮新增 4 模型里暂时还没有新增出一个“已进入有效输入协议”的正式主表候选。
- `Orca-2-7B` 单模型 provisional `val_calib50` 已完成，汇总输出为：
  - `outputs/provisional/calibration_report.labels-orca-2-7b.provisional.csv`
- 已将 Orca 旧 calibration run 归档到：
  - `outputs/provisional/archive/orca_calibration_before_llamatokenizer_20260404/Orca-2-7B/`
- Orca 单模型 provisional `val_calib50` 三档输出位置：
  - `outputs/calibration_runs/Orca-2-7B/max_new_tokens_128/summary.json`
  - `outputs/calibration_runs/Orca-2-7B/max_new_tokens_192/summary.json`
  - `outputs/calibration_runs/Orca-2-7B/max_new_tokens_256/summary.json`
  - 已落盘的样本记录里能看到 `prompt_wrapper_family=chatml`、`effective_tokenizer_class_name=LlamaTokenizer`
  - 已看到至少一条 `Answer: -16.34%` 且 `valid_parse=True`
- Orca `max_new_tokens_128` 的首档 provisional summary：
  - `answer_present_rate=0.8`
  - `valid_parse_rate=0.72`
  - `truncation_without_answer_rate=0.18`
  - `mean_new_tokens=33.72`
  - `p95_new_tokens=128`
- 解释边界：这说明 Orca 修复已从 1 条 smoke 外推到 50 条规模后仍能产出较多合法答案，但 `128` 档截断率仍明显过高，不能据此冻结长度或写入 canonical 主表。
- Orca `max_new_tokens_192` 的第二档 provisional summary：
  - `answer_present_rate=0.86`
  - `valid_parse_rate=0.78`
  - `truncation_without_answer_rate=0.1`
  - `mean_new_tokens=42.84`
  - `p95_new_tokens=192`
- 当前解释：`192` 档比 `128` 有改善，但 10% 的 `truncation_without_answer_rate` 仍高于当前安全口径，因此还不能据此冻结长度，必须继续看 `256` 档。
- Orca `max_new_tokens_256` 的第三档 provisional summary：
  - `answer_present_rate=0.86`
  - `valid_parse_rate=0.78`
  - `truncation_without_answer_rate=0.1`
  - `mean_new_tokens=49.24`
  - `p95_new_tokens=256`
- 关键结论：
  - Orca 的 `chatml + LlamaTokenizer` 修复**确实显著优于旧 run**，但 `128/192/256` 三档都没有把 `truncation_without_answer_rate` 压到当前 2% 安全口径
  - `256` 档里仍有 `5/50` 条样本吃满 256 token 且没有 `Answer:`，例如 `MSI/2006/page_61.pdf-1`、`CAT/2017/page_85.pdf-3`、`GIS/2012/page_64.pdf-1`
  - 这些样本的 `prediction_text` 前缀都是“先分步骤解释”，说明当前主要问题是 Orca 仍倾向输出长推理文本而不是直接收束到最终答案行
- 接下来优先执行不需要大量流量的本地任务：
  - 先分析 Orca `max_new_tokens_256` 这 5 条 `truncated_suspect=True` 样本，判断是 wrapper 仍不够约束，还是必须在 provisional 层继续扩到 `320/384`
  - `LaMini-LLaMA-7B` 继续保留为待补 repo_id 占位，不下载、不 smoke
- `Xwin-LM-7B` 补下载已完成：
  - 默认 xet 路径下曾出现“进程存活但 `.incomplete` 字节数长期不增长”
  - 已改用 `HF_HUB_DISABLE_XET=1` 走普通 HTTP 续传
  - 当前 Xwin cache 体积约 `18G`
  - `/Users/sam/.cache/huggingface/hub/models--Xwin-LM--Xwin-LM-7B-V0.1` 下已无 `.incomplete/.lock`
  - `outputs/metadata/provisional/model_registry.groups-main-candidate-expansion.json` 中 `Xwin-LM-7B` 已更新为 `available_local`

## 待完成
- [[实验协议_v1]]
- [[长度校准报告]]
- [[准入筛选总结]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_主表扩展与新增模型筛查]]
- [[下一步]]

## 关键约束
- `test1147` 仅用于协议冻结后的最终一次性正式评估。
- 今晚至少交付四样结果：原始 split、`protocol_v1`、`calibration_report.csv`、`qualification_summary.csv`。
- `qa.answer` 与 `qa.exe_ans` 不可混用。validation 中有 `623/883` 条数值不一致，主要来自百分比缩放和四舍五入差异。
- 官方 split 含少量 `yes/no` 问题，现已在 `gold_numeric` 中统一映射为 `yes -> 1.0`、`no -> 0.0`，但 `raw_answer` 保留原样。
- 事件与解决方案统一记录在 [[异常与决策记录]]。
- 统一模型路径与来源在 [[模型注册表]] 中维护，不再手写。
- `Lion-7B` 的 1 条 smoke 已验证：在 `max_new_tokens=256` 下会出现“吃满上限但无 `Answer:`”的情况，当前协议已修正为会正确记作 `truncated_suspect`。
- `Lion-7B` smoke 还显示本机吞吐约 `2.89 tok/s`，这意味着若坚持今晚跑完 6 模型全流程，时间上不可行；已在 [[异常与决策记录]] 中登记并提出“主表优先”的解决方案。
- `filtered` 范围的 registry / calibration / qualification 输出现默认走 `provisional` 路径，不再覆盖 canonical 文件。
- canonical 旧 6 模型注册表里，Lion/Orca/Zephyr/MiniLLM/DeepSeek/OpenR1 仍均为 `available_local`。
- 本轮新增扩表的 provisional registry 中，`Xwin-LM-7B`、`DeepSeek-R1-Distill-Qwen-7B`、`OpenR1-Distill-7B` 均为 `available_local`，`LaMini-LLaMA-7B` 仍是 `pending_repo_id`。
- 当前 `calibration_report.csv` 显示：
  - `128 / 192 / 256` 三档都无法满足“全部主表模型 `truncation_without_answer_rate <= 2%`”
  - `Lion-7B` 在三档下均为 `1.0`
  - 因此当前协议下没有可冻结的安全档位
- 在新的长度决策确认前，不能进入正式主表 `val_screen200`
- 本轮 wrapper registry v2 smoke 的新结论：
  - `Zephyr-7B-beta -> plain` 是目前唯一 `Answer:=True` 且 `valid_parse=True` 的主表路径，可视为已进入“有效输入协议”
  - `Lion-7B -> vicuna`：special-token pad 对齐已修复，但仍无 `Answer:`、`truncation_suspect=True`、`repetition_collapse=True`
  - `Orca-2-7B -> chatml`：special-token pad 对齐已修复，但仍无 `Answer:`、`truncation_suspect=True`
  - `MiniLLM-Llama-7B -> completion`：special-token pad 对齐已修复，但仍无 `Answer:`，更适合暂时保留为 `base_distilled` baseline，而不是直接进入统一 instruction 主表
- 旧失败尝试已整理到 [[过程总结_输入包装修复]]，旧 v1 debug 文件已归档到 `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/`
- v3 最小修复结论：
  - `Lion-7B -> alpaca`：prompt 序列化已切到官方 Alpaca-style，但这条样本仍无 `Answer:`、`valid_parse=False`、`truncation_suspect=True`、`repetition_collapse=True`
  - `Orca-2-7B -> chatml + LlamaTokenizer`：这条样本已输出 `Answer: 1.33%`，`valid_parse=True`，`truncation_suspect=False`
  - 因此 Orca 旧失败结论现在开始更有解释价值；Lion 的“vicuna 失败”结论不能再直接引用，应以 v3 Alpaca 结果为准
- 当前**仍不建议立刻重跑 canonical `val_calib50`**：
  - 原因是 Lion 虽已切到 Alpaca wrapper，但仍未进入有效输入协议
  - 如果要推进，可先做 **Orca 单模型 provisional `val_calib50`**，不要直接恢复全主表 canonical calibration
- MiniLLM-Llama-7B 仍建议保持 `base_distilled` baseline 定位，不在本轮继续调 wrapper
- 当前正式 instruction 主表候选仍只有 `Orca-2-7B` 和 `Zephyr-7B-beta` 两个“已进入有效输入协议”的模型，离“不少于 4 个”还差 `2` 个。
- 当前 2026-04-07 新增 6 模型预筛里的已知前提：
  - `Llama-3.1-8B-Instruct` 与 `Gemma-7B-IT` 的主要阻塞已不是 smoke，而是官方 gated repo 访问权限
  - 因此最终能否补齐到“Zephyr + Orca + 4 个新增候选 = 6 模型”，当前关键取决于剩余 4 个非门控模型的 smoke 结果
- `Qwen2.5-7B-Instruct` 已完成官方下载与 1 条 smoke：
  - provisional registry：`outputs/metadata/provisional/model_registry.labels-qwen2-5-7b-instruct.json`
  - smoke 输出：`outputs/debug/input_smoke_mainstream/Qwen2.5-7B-Instruct/smoke_mainstream.json`
  - 当前结果：`effective_wrapper=chat_template`、`effective_tokenizer_class_name=Qwen2Tokenizer`
  - `answer_appears=True / valid_parse=True / truncation_suspect=False / repetition_collapse=False`
  - 因此 `Qwen2.5-7B-Instruct` 是本轮新增主流模型里第一个实际通过 smoke 的模型，已进入“有效输入协议”
- 当前 2026-04-07 新增主流模型 smoke 预筛进度更新为：
  - 已完成：`Mistral-7B-Instruct-v0.3`、`Qwen2.5-7B-Instruct`、`Yi-1.5-6B-Chat`
  - 官方 gated：`Llama-3.1-8B-Instruct`、`Gemma-7B-IT`
  - `ChatGLM3-6B`：已下载完成，但首条 smoke 当前卡在加载入口
- 截至当前，已进入有效输入协议的模型扩展为：
  - 旧候选：`Orca-2-7B`、`Zephyr-7B-beta`
  - 新增主流模型：`Qwen2.5-7B-Instruct`、`Yi-1.5-6B-Chat`
  - 合计：`4` 个
- “截至当前全部已尝试模型”的汇总表已生成：
  - `outputs/debug/protocol_screening_registry/all_attempted_models_summary.json`
  - `outputs/debug/protocol_screening_registry/all_attempted_models_summary.md`
- 汇总脚本的一个取数缺口已修复：
  - 旧逻辑只读 `outputs/debug/input_smoke_mainstream/summary.json`

## 2026-04-07 最新更新：6 模型 provisional calibration 已完成并修复
- fresh run root：
  - `outputs/provisional/calibration_runs_6model_20260407/`
- fresh aggregate csv：
  - `outputs/provisional/calibration_report.6model_mainstream_20260407.csv`
- 最终结果：
  - `Orca-2-7B = 0.18 / 0.10 / 0.10`
  - `Zephyr-7B-beta = 0.32 / 0.14 / 0.06`
  - `Qwen2.5-7B-Instruct = 0.00 / 0.00 / 0.00`
  - `Mistral-7B-Instruct-v0.3 = 0.00 / 0.00 / 0.00`
  - `Yi-1.5-6B-Chat = 0.02 / 0.02 / 0.02`
  - `ChatGLM3-6B = 0.46 / 0.04 / 0.04`
- 修复记录：
  - `ChatGLM3-6B / 128` 初版曾出现 `summary/report=50` 但 `predictions=44`
  - 已归档旧版本到 `outputs/provisional/archive/chatglm3-6b-max128-suspect-20260407/`
  - 已单独重跑 `ChatGLM3-6B / 128`
  - 当前所有 completed 档位都满足：`predictions.jsonl` 行数等于 `summary.json.num_examples`
- 当前结论：
  - 这份 aggregate csv 现在可作为“已修复完成的 provisional calibration 报表”使用
  - 但 `128 / 192 / 256` 三档仍没有一个能让全部 6 模型同时满足 `truncation_without_answer_rate <= 0.02`
  - 该文件实际是“最后一次 smoke 覆盖写”，不适合做全量汇总
  - 现已改成逐模型读取 `outputs/debug/input_smoke_mainstream/<model>/smoke_mainstream.json`
  - 修复后全量总表已正确回填 `Mistral / Qwen2.5 / Yi` 的 smoke 结果
- `Yi-1.5-6B-Chat` 已完成官方下载与 1 条 smoke：
  - provisional registry：`outputs/metadata/provisional/model_registry.labels-yi-1-5-6b-chat.json`
  - smoke 输出：`outputs/debug/input_smoke_mainstream/Yi-1.5-6B-Chat/smoke_mainstream.json`
  - 当前结果：`effective_wrapper=chat_template`、`effective_tokenizer_class_name=TokenizersBackend`
  - `answer_appears=True / valid_parse=True / truncation_suspect=False / repetition_collapse=False`
  - 因此 `Yi-1.5-6B-Chat` 已进入“有效输入协议”
- `ChatGLM3-6B` 已开始官方下载：
  - 现已完成下载，provisional registry：`outputs/metadata/provisional/model_registry.labels-chatglm3-6b.json`
  - 首条 smoke 已执行，但结果不是协议层 pass/fail，而是 `load_failed`
  - 直接报错：`AttributeError: 'ChatGLMConfig' object has no attribute 'max_length'`
  - 因此当前阻塞更像 `trust_remote_code` / ChatGLM 自定义加载入口与现有 smoke runner 的兼容性问题，而不是 parser 问题

## 当前新阻塞
- `ChatGLM3-6B` 当前不能直接算作 smoke 协议失败：
  - 官方快照已下载完成
  - 但当前 smoke runner 在模型加载阶段报 `ChatGLMConfig.max_length` 缺失
  - 这说明下一步不该改 parser，也不该改 benchmark 任务语义
  - 更合理的最小动作是：先修 `ChatGLM3-6B` 的 `trust_remote_code` 加载/生成入口，再重跑 1 条 smoke
- 当前**仍不建议恢复 canonical `val_calib50` 或启动正式主表 `val_screen200`**，因为 Orca 虽可解析率明显恢复，但 `256` 档截断率仍有 10%，新增 4 模型又暂未补出新的有效 instruction 候选。
- Xwin-LM-7B 补跑 1 条 smoke 的新结论：
  - `outputs/debug/input_smoke_expansion/Xwin-LM-7B/smoke_expansion.json` 显示 `effective_wrapper=vicuna`、`answer_appears=False`、`valid_parse=False`、`truncation_suspect=True`
  - 底层 token probe 进一步确认模型连续生成 `128` 个 token id `0`，而该 tokenizer 中 `0 == <unk> == pad_token_id`
  - 因此 `decode(skip_special_tokens=True)` 后得到空串，Xwin 当前失败形态应记为“`<unk>` 重复退化”，仍未进入有效输入协议
- Orca `256` 档 5 条截断样本复查结论：
  - 5 条样本分别是 `MSI/2006/page_61.pdf-1`、`CAT/2017/page_85.pdf-3`、`GIS/2012/page_64.pdf-1`、`MRO/2006/page_33.pdf-2`、`ILMN/2007/page_78.pdf-2`
  - 5/5 都是 `prompt_wrapper_family=chatml`、`prompt_render_mode_effective=chatml`、`effective_tokenizer_class_name=LlamaTokenizer`
  - 5/5 的 `prediction_text` 都以 `To calculate...` / `To answer this question...` 开头，先进入自然语言分步推理，吃满 `256` token 后仍没有 `Answer:` 行，因此 `parse_error_reason=missing_answer_line`
  - 非截断样本里 `45` 条中有 `40` 条直接以 `Answer:` 开头，说明 Orca 剩余 5 条失败更像“模型在个别样本上违背 direct-answer 指令，转成长推理模式”，而不是一个统一的 ChatML wrapper / tokenizer 损坏问题
  - 截断组 `prompt_length_tokens` 的 `min/mean/max=958/1416.0/1633`，非截断组是 `543/1129.1/1736`，非截断组最大 prompt 反而更长，因此这 5 条不是单纯“输入太长把输出空间挤没了”
- DeepSeek / OpenR1 下一步最小动作已经收束为“先查官方 wrapper”，不继续在当前失败路径上重复 smoke：
  - `DeepSeek-R1-Distill-Qwen-7B` 当前更像“长推理不收束 / 无 Answer 行”
  - `OpenR1-Distill-7B` 当前更像“plain wrapper 不合适 + 回显 `Answer: <numeric value>` 占位符”
- DeepSeek / OpenR1 官方 wrapper 初查新结论：
  - `DeepSeek-R1-Distill-Qwen-7B` 本地 `tokenizer_config.json` 的 `chat_template` 会在 assistant 边界后直接追加 `<｜Assistant｜><think>\\n`；[官方模型卡](https://huggingface.co/deepseek-ai/DeepSeek-R1-Distill-Qwen-7B) 又明确建议不要加 system prompt、数学题答案放 `\\boxed{}`、并强制以 `<think>\\n` 开头，所以它的官方推荐用法和当前“无 CoT + 只吃 `Answer:` 行”的主协议存在结构性冲突。
  - `OpenR1-Distill-7B` 本地 `tokenizer_config.json` 没有 `chat_template`，但保留 `<|im_start|>/<|im_end|>` special tokens；[官方模型卡](https://huggingface.co/open-r1/OpenR1-Distill-7B) 推荐 `transformers chat` 或 `pipeline(messages)` 的 user-message 入口，没有示范手写 plain 拼接，因此当前 plain 回显占位符大概率不是 parser 问题，而是序列化方式不够官方。

## 当前明确不再重复做什么
- 不再重跑 `Xwin-LM-7B` 当前 Vicuna 路径下同样的 1 条 smoke。
- 不再继续把 `Lion-7B` 拉回正式主表。
- 不再继续调 `MiniLLM-Llama-7B` wrapper。
- 不恢复 canonical `val_calib50`。
- 不恢复 canonical `val_screen200`。
- 不改 parser。
- 不把 benchmark 主协议改成 CoT。

## 相关入口
- [[项目总览]]
- [[任务笔记 - FinQA 基准重建]]
- [[会话日志]]
- [[下一步]]
- [[异常与决策记录]]
- [[模型注册表]]
- [[组员运行说明]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_协议可用性筛查]]
- [[过程总结_orca_下一步决策]]
- [[过程总结_新增候选失败类型分类]]

## 2026-04-07 09:54 HKT 最新更新
- `ChatGLM3-6B` 的 `trust_remote_code` 兼容修复已完成，并已重跑同一条 smoke：
  - 输出：`outputs/debug/input_smoke_mainstream/ChatGLM3-6B/smoke_mainstream.json`
  - 当前结果：`effective_wrapper=chat_template`
  - `effective_tokenizer_class_name=ChatGLMTokenizer`
  - `answer_appears=True / valid_parse=True / truncation_suspect=False / repetition_collapse=False`
  - `raw_generation_text=Answer: 17.8`
- 这次修复只涉及“生成入口兼容层”，不涉及 benchmark 主协议：
  - 没改 parser
  - 没改 direct-answer 语义
  - 没改 canonical 结果
  - 没引入 CoT
- 当前 `Lion` 环境里的 `transformers=5.1.0`，而 `ChatGLM3-6B` 远端实现依赖旧 generation 私有接口；因此旧结论“ChatGLM3 load_failed”不应再直接引用为协议失败证据。
- 截至当前，已进入有效输入协议的模型增至 `5` 个：
  - `Orca-2-7B`
  - `Zephyr-7B-beta`
  - `Qwen2.5-7B-Instruct`
  - `Yi-1.5-6B-Chat`
  - `ChatGLM3-6B`
- 当前仍不恢复 canonical 主线：
  - 最终 6 模型名单目前仍差 `1`
  - `Llama-3.1-8B-Instruct` 与 `Gemma-7B-IT` 仍是官方 gated
  - `Mistral-7B-Instruct-v0.3`、`Xwin-LM-7B`、`DeepSeek-R1-Distill-Qwen-7B`、`OpenR1-Distill-7B`、`Lion-7B`、`MiniLLM-Llama-7B` 仍各有未解问题

## 2026-04-07 10:xx HKT 正在运行
- 已按“`Orca + Zephyr + Qwen2.5 + Mistral + Yi + ChatGLM3` 共 6 模型”启动 fresh `val_calib50` calibration。
- 本轮目标：
  - 在 `128 / 192 / 256` 三档下分别计算 6 模型的 `truncation_without_answer_rate`
  - 判断是否存在一个统一档位，能让全部 6 模型同时降到可接受范围
- 当前运行策略：
  - 只写 `provisional`
  - 不覆盖旧 `outputs/calibration_runs/`
  - 不覆盖 canonical `outputs/calibration_report.csv`
- 本轮新输出位置：
  - run root：`outputs/provisional/calibration_runs_6model_20260407/`
  - aggregate csv：`outputs/provisional/calibration_report.6model_mainstream_20260407.csv`
  - registry：`outputs/metadata/provisional/model_registry.labels-orca-2-7b-zephyr-7b-beta-qwen2-5-7b-instruct-mistral-7b-instruct-v0-3-yi-1-5-6b-chat-chatglm3-6b.json`
- 这次能 fresh 跑 `ChatGLM3-6B` calibration 的前提是：
  - 已把 smoke 中验证过的最小 compat 逻辑同步到 `run_finqa_local_benchmark_v1.py`
  - 否则 calibration runner 会再次把 ChatGLM 的入口兼容问题误记成 runtime failure
- 当前进度：
  - `Orca-2-7B / 128` 已完成，`truncation_without_answer_rate=0.18`
  - `Orca-2-7B / 192` 已完成，`truncation_without_answer_rate=0.10`
  - `Orca-2-7B / 256` 已完成，`truncation_without_answer_rate=0.10`
  - `Zephyr-7B-beta / 128` 已完成，`truncation_without_answer_rate=0.32`
  - `Zephyr-7B-beta / 192` 已完成，`truncation_without_answer_rate=0.14`
  - `Zephyr-7B-beta / 256` 已完成，`truncation_without_answer_rate=0.06`
  - `Qwen2.5-7B-Instruct` 三档已完成，`truncation_without_answer_rate=0.00 / 0.00 / 0.00`
  - `Mistral-7B-Instruct-v0.3` 三档已完成，`truncation_without_answer_rate=0.00 / 0.00 / 0.00`
  - `Yi-1.5-6B-Chat` 三档已完成，`truncation_without_answer_rate=0.02 / 0.02 / 0.02`
  - `ChatGLM3-6B / 192` 已完成，`truncation_without_answer_rate=0.04`
  - `ChatGLM3-6B / 256` 已完成，`truncation_without_answer_rate=0.04`
  - aggregate csv 已生成：`outputs/provisional/calibration_report.6model_mainstream_20260407.csv`
  - 但 `ChatGLM3-6B / 128` 当前存在结果一致性问题：
    - aggregate csv 与 `summary.json` 都写成 `num_examples=50`
    - `predictions.jsonl` 实际只有 `44` 行
- 当前状态判断：
  - 后台 calibration 进程已结束，当前不是“还在跑”，而是“已产出 provisional 报表”
  - `Zephyr-7B-beta` 三档正式结果已经证明：smoke 通过并不等于 length calibration 安全
  - 即使升到 `256`，`Zephyr-7B-beta` 的正式 `truncation_without_answer_rate` 仍是 `0.06`，仍高于“全模型共同安全”的 `2%` 口径
  - 目前前 5 个模型里，`Qwen2.5-7B-Instruct`、`Mistral-7B-Instruct-v0.3` 已经显示出稳定的 `0.00` 截断率，`Yi-1.5-6B-Chat` 也明显好于 Orca/Zephyr
  - 但在 `ChatGLM3-6B / 128` 的证据文件不一致被修复前，这份 aggregate csv 仍只能记作 `provisional_suspect`，不能直接冻结统一长度

## 2026-04-07 15:xx HKT 最新更新：冻结 6 模型 val_screen200
- 已在 [[实验协议_v1]] 冻结本轮 `val_screen200`：
  - `max_new_tokens = 256`
  - `runtime_success >= 0.95`
  - `format_ok >= 0.80`
  - `valid_parse >= 0.60`
  - `truncation_without_answer_rate <= 0.10`
- 已生成本轮 6 模型 provisional registry：
  - `outputs/metadata/provisional/model_registry.labels-orca-2-7b-zephyr-7b-beta-qwen2-5-7b-instruct-mistral-7b-instruct-v0-3-yi-1-5-6b-chat-chatglm3-6b.json`
- 已初始化新的 `200` 条目录：
  - `outputs/provisional/screen200_6model_20260407/`
- 已初始化实时进度表：
  - `outputs/provisional/screen200_6model_20260407/screen200_status_table.md`
  - `outputs/provisional/screen200_6model_20260407/screen200_status_table.json`
- 当前执行方式：
  - 严格逐模型运行
  - 每跑完一个模型就刷新一次 `screen200_status_table`
  - 全程使用 `--screen-only --provisional`
- 当前即将启动的首个模型：
  - `Orca-2-7B`
- `screen200_status_table` 已增强为：
  - 即使 `summary.json` 还没落盘，也会显示 `running_no_summary`
  - 并在 `note` 中记录当前 `predictions_lines=<已写行数>`
- 当前实时进度：
  - `Orca-2-7B` 已完成 `val_screen200`
  - 当前按用户要求暂停在单模型阶段，不继续后续 5 个模型

## 2026-04-07 增量落盘规则
- 从这一刻开始，不再采用“单模型跑完后再统一补写”的方式。
- 改为每个模型至少分三次写入 markdown：
  - 启动时写一次
  - 中间出现已落盘样本时写一次
  - 完成并刷新表时再写一次
- 目的：
  - 防止网络或终端中断时，实验过程没有落盘证据
  - 保证 `[[当前状态]]`、`[[会话日志]]`、日期日志能持续追踪到逐模型进度

## 2026-04-07 当前 screen200 停止点
- 已完成且保留的模型：
  - `Orca-2-7B`
- `Orca-2-7B` 当前结果：
  - `runtime_success = 1.000`
  - `format_ok = 0.960`
  - `valid_parse = 0.845`
  - `truncation_without_answer_rate = 0.025`
  - `EM = 0.030`
  - `TM = 0.075`
  - 结论：通过冻结门槛
- 已停止的模型：
  - `Zephyr-7B-beta`
- `Zephyr-7B-beta` 只跑了极少量样本后已按用户要求停止：
  - 活跃目录中的半截结果已移出
  - 归档位置：`outputs/provisional/archive/zephyr-screen200-partial-20260407/Zephyr-7B-beta/`
- 当前活跃汇总表只保留：
  - `Orca-2-7B = ok / yes`
  - 其余模型仍是 `pending`

## 2026-04-07 恢复运行前检查
- 用户已确认继续后续模型。
- 当前检查到：
  - 活跃目录里的 `Zephyr-7B-beta` 已有中间产物
  - `predictions.jsonl` 当前为 `89/200`
  - 但外层 `run_qualification_v1.py` 活进程已不存在
  - `summary.json` 仍未生成
- 当前判断：
  - 这是一份“已写到中途但未完成”的中间证据
  - 不能直接当正式 screening 结果
  - 需要在保留该证据的前提下，决定是否继续从头补跑

## 2026-04-07 最新恢复结论
- 继续排查后确认：
  - `Zephyr-7B-beta` 并没有真正停止
  - 外层 `run_qualification_v1.py` 已退场
  - 但内层 `run_finqa_local_benchmark_v1.py` 仍在继续写
- 当前内层活进程：
  - `PID 60911`
- 当前实时进度：
  - `Zephyr-7B-beta` 已写入约 `181/200`
  - `summary.json` 仍未生成
- 当前处理策略：
  - 不重新开第二个 `Zephyr` 命令
  - 直接监控现有 benchmark 进程
  - 按增量落盘规则继续写日志和刷新 `screen200_status_table`

## 2026-04-07 Zephyr 结果一致性异常
- `Zephyr-7B-beta` 当前已退出内层 benchmark 进程，并生成了 `summary.json`。
- 但出现了会影响结果解释的一致性问题：
  - `summary.json.num_examples = 200`
  - `predictions.jsonl` 当前只有 `195` 行
- 当前 `summary.json` 中写出的指标为：
  - `runtime_success = 1.000`
  - `format_ok = 0.430`
  - `valid_parse = 0.095`
  - `truncation_without_answer_rate = 0.105`
  - `EM = 0.010`
  - `TM = 0.020`
- 由于 `num_examples` 与逐样本文件不一致，这份 `Zephyr` 结果当前只能记为：
  - `provisional_suspect`
- 在修复前：
  - 不将 `Zephyr-7B-beta` 记入正式 `screen200` 通过/不通过表
  - 不继续启动下一个模型
- 当前最小修复动作：
  - 归档当前 `Zephyr-7B-beta` 目录
  - 单独重跑 `Zephyr-7B-beta / val_screen200 / 256`
  - 重建 `screen200_status_table`

## 2026-04-07 Zephyr 最小修复已获批准
- 用户已同意执行最小修复。
- 当前修复范围仅限：
  - `Zephyr-7B-beta / val_screen200 / 256`
- 当前不做的事：
  - 不继续下一个模型
  - 不改 parser
  - 不改冻结门槛
  - 不改 canonical

## 2026-04-07 Zephyr 修复进展
- 当前可疑目录已归档：
  - `outputs/provisional/archive/zephyr-screen200-suspect-20260407T193617/Zephyr-7B-beta/`
- 当前活跃目录已清除旧 `Zephyr` 产物。
- 当前活跃总表已回到：
  - `Orca-2-7B = ok / yes`
  - `Zephyr-7B-beta = pending`
- 下一步：
  - 单独重跑 `Zephyr-7B-beta / val_screen200 / 256`

## 2026-04-07 Zephyr 重跑已启动
- 当前已重新启动：
  - `Zephyr-7B-beta / val_screen200 / 256`
- 当前状态：
  - 本次重跑已完成
  - `predictions.jsonl = 200`
  - `summary.json.num_examples = 200`
  - 一致性已修复

## 2026-04-07 Zephyr 修复后正式结果
- `Zephyr-7B-beta` 当前正式 `screen200` 结果：
  - `runtime_success = 1.000`
  - `format_ok = 0.430`
  - `valid_parse = 0.095`
  - `truncation_without_answer_rate = 0.105`
  - `EM = 0.010`
  - `TM = 0.020`
- 结论：
  - 一致性问题已修复
  - 但模型本身 **未通过** 当前冻结门槛
  - 失败原因不是“证据文件损坏”，而是指标本身不足：
    - `format_ok < 0.80`
    - `valid_parse < 0.60`
    - `truncation_without_answer_rate > 0.10`
- 当前活跃总表状态：
  - `Orca-2-7B = ok / yes`
  - `Zephyr-7B-beta = ok / no`
- 下一步：
  - 启动 `Mistral-7B-Instruct-v0.3 / val_screen200 / 256`

## 2026-04-07 Mistral 已启动
- 当前已启动：
  - `Mistral-7B-Instruct-v0.3 / val_screen200 / 256`
- 当前状态：
  - 外层 `run_qualification_v1.py` 进程正常
  - 新的 `predictions.jsonl` 已开始写
  - 当前约 `65/200`
  - `summary.json` 尚未生成
