# 当前状态

## 基本信息
- 更新时间：2026-04-04 HKT
- 当前阶段：协议可用性筛查后的收束分析已完成，当前 8 个模型中只有 `2` 个进入有效输入协议
- 当前目标：不恢复 canonical 主线的前提下，先完成 DeepSeek / OpenR1 官方 wrapper 初查，并按 [[过程总结_新增候选失败类型分类]] 收束下一步是否值得做“官方 messages 序列化”对照 smoke
- 工作目录：`/Users/sam/Documents/Documents - sam的MacBook Pro/论文📑/Distillation/new task`
- 环境：`conda run -n Lion`

## 已完成
- 确认 `new task` 目录存在且可用。
- 确认 `Lion` conda 环境存在。
- 初始化基础目录：`docs/`、`logs/`、`outputs/`、`data/`、`scripts/`。
- 建立首批项目文档与日志入口。
- 从官方 GitHub 成功重拉 `train/dev/test` 三个原始 split。
- 生成标准化数据：
  - `data/processed/finqa/train.standardized.jsonl`
  - `data/processed/finqa/validation.standardized.jsonl`
  - `data/processed/finqa/test.standardized.jsonl`
- 生成三个固定 manifest：
  - `data/manifests/val_calib50.jsonl`
  - `data/manifests/val_screen200.jsonl`
  - `data/manifests/validation883.jsonl`
- 输出字段核对与 manifest 元数据：
  - `outputs/metadata/finqa_split_summary.csv`
  - `outputs/metadata/finqa_manifest_index.csv`
- 冻结了协议代码与机器可读 bundle：
  - `scripts/finqa_protocol_v1.py`
  - `outputs/protocol_v1/protocol_v1.json`
  - `outputs/protocol_v1/prompt_system_v1.txt`
  - `outputs/protocol_v1/prompt_user_v1.txt`
  - `outputs/protocol_v1/decode_config_v1.json`
- 实现了统一 runner 和两个汇总脚本：
  - `scripts/run_finqa_local_benchmark_v1.py`
  - `scripts/run_length_calibration_v1.py`
  - `scripts/run_qualification_v1.py`
- 新增单模型运行与环境固化资产：
  - `scripts/run_assigned_model_v1.sh`
  - `requirements-benchmark.txt`
- 三个入口脚本现已支持 `--labels`，可按精确模型名单跑：
  - `scripts/ensure_candidate_models.py`
  - `scripts/run_length_calibration_v1.py`
  - `scripts/run_qualification_v1.py`
- `scripts/run_qualification_v1.py` 现已支持 `--screen-only`，可只执行 `val_screen200`
- 新增组员执行说明：[[组员运行说明]]
- 新增了模型状态入口：[[模型注册表]]
- 已用 `Lion-7B` 跑通 1 条 smoke，确认 runner 能产出 summary / predictions 文件。
- 已将 `Lion` 环境固定为 Orca 可加载的版本组合：
  - `protobuf == 3.20.3`
  - `tiktoken == 0.12.0`
- 已验证 `Orca-2-7B` tokenizer 可以正常初始化。
- 已用 `Orca-2-7B` 跑通 1 条 smoke，确认完整 runner 链路恢复：
  - `outputs/smoke/orca_smoke_predictions_v1.jsonl`
  - `outputs/smoke/orca_smoke_summary_v1.json`
- 已恢复 canonical `outputs/metadata/model_registry.json/csv`。
- 已将被污染的旧 `outputs/calibration_report.csv` 归档到：
  - `outputs/provisional/archive/calibration_report.pre_restore.invalid.csv`
- canonical `outputs/calibration_report.csv` 已生成。
- 新增输入接口专项诊断文档：[[输入接口诊断]]
- 已实现并运行旧 v1 输入接口诊断与 1 条 smoke 对照，旧输出已归档：
  - `scripts/debug_input_interface_v1.py`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_interface_diagnosis/*.json`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_interface_diagnosis/*.txt`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_smoke/*/smoke_compare.json`
  - `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/input_smoke/*/smoke_compare.txt`
- 新增 wrapper registry v2 与 special-token alignment 修复逻辑：
  - `scripts/finqa_protocol_v1.py`
  - `scripts/run_finqa_local_benchmark_v1.py`
  - `scripts/debug_wrapper_registry_v2.py`
- 已生成 wrapper registry v2 provisional 输出：
  - `outputs/debug/wrapper_registry/registry_index.json`
  - `outputs/debug/wrapper_registry/*.json`
  - `outputs/debug/wrapper_registry/*.txt`
  - `outputs/debug/input_smoke_v2/*/smoke_v2.json`
  - `outputs/debug/input_smoke_v2/*/smoke_v2.txt`
  - `outputs/debug/input_smoke_v2/summary.json`
- 新增过程总结文档：[[过程总结_输入包装修复]]
- 新增协议筛查收束文档：
  - [[过程总结_协议可用性筛查]]
  - [[过程总结_orca_下一步决策]]
  - [[过程总结_新增候选失败类型分类]]
  - `outputs/debug/protocol_screening_registry/current_candidate_status_table.json`
  - `outputs/debug/protocol_screening_registry/current_candidate_status_table.md`
- 已复核官方输入格式来源：
  - Lion 官方 `train.py` 使用 Alpaca-style `### Instruction / ### Response`
  - Orca 官方模型卡要求 ChatML 且 `use_fast=False`
  - MiniLLM 官方资料没有给单一固定 inference wrapper
- 已完成 v3 最小修复与 2 模型 1 条 debug smoke：
  - `scripts/debug_wrapper_registry_v3.py`
  - `outputs/debug/input_smoke_v3/Lion-7B/smoke_v3.json`
  - `outputs/debug/input_smoke_v3/Orca-2-7B/smoke_v3.json`
  - `outputs/debug/input_smoke_v3/summary.json`
- 已归档 v3a 旧偏差输出：
  - `outputs/debug/archive/debug_failed_attempts/input_smoke_v3a_orca_autotokenizer_use_fast_false_not_effective_20260404/input_smoke_v3/`

## 进行中
- 主表 screening 继续暂停。
- 本轮只分析 `outputs/debug/` 下的 provisional smoke / diagnosis，不使用这些结果覆盖 canonical 主表。
- 已开始主表扩展筛查，但因当前网络切到热点，`Xwin-LM-7B` 下载已按用户指令暂停，partial cache 保留在 HF cache 中。
- 已新增 `scripts/ensure_candidate_models.py --local-only`，可只扫描本地 HF cache 生成 provisional registry，不再触发远程 `model_info()` / 下载请求。
- 已离线生成新增候选池 provisional registry：
  - `outputs/metadata/provisional/model_registry.groups-main-candidate-expansion.json`
  - `outputs/metadata/provisional/model_registry.groups-main-candidate-expansion.csv`
- 已完成新增 4 模型的 expansion debug / smoke 本地首轮输出：
  - `outputs/debug/wrapper_registry_expansion/*.json`
  - `outputs/debug/input_interface_diagnosis_expansion/*.json`
  - `outputs/debug/input_smoke_expansion/*/smoke_expansion.json`
  - `outputs/debug/input_smoke_expansion/summary.json`
- expansion smoke 首轮结论：
  - `Xwin-LM-7B`：`incomplete_local_snapshot`，因 hotspot 暂停下载导致本地 cache 仍有 `.incomplete` blob，本轮跳过 smoke
  - `LaMini-LLaMA-7B`：`pending_repo_id`，本轮跳过 smoke
  - `DeepSeek-R1-Distill-Qwen-7B -> chat_template`：`Answer=False / valid_parse=False / truncation_suspect=True`
  - `OpenR1-Distill-7B -> plain`：`Answer=True` 但 `answer_line_raw='Answer: <numeric value>'`，`valid_parse=False / parse_error_reason=invalid_numeric_literal`
- 因此本轮新增 4 模型里暂时还没有新增出一个“已进入有效输入协议”的正式主表候选。
- `Orca-2-7B` 单模型 provisional `val_calib50` 已完成，汇总输出为：
  - `outputs/provisional/calibration_report.labels-orca-2-7b.provisional.csv`
- 已将 Orca 旧 calibration run 归档到：
  - `outputs/provisional/archive/orca_calibration_before_llamatokenizer_20260404/Orca-2-7B/`
- Orca 单模型 provisional `val_calib50` 三档输出位置：
  - `outputs/calibration_runs/Orca-2-7B/max_new_tokens_128/summary.json`
  - `outputs/calibration_runs/Orca-2-7B/max_new_tokens_192/summary.json`
  - `outputs/calibration_runs/Orca-2-7B/max_new_tokens_256/summary.json`
  - 已落盘的样本记录里能看到 `prompt_wrapper_family=chatml`、`effective_tokenizer_class_name=LlamaTokenizer`
  - 已看到至少一条 `Answer: -16.34%` 且 `valid_parse=True`
- Orca `max_new_tokens_128` 的首档 provisional summary：
  - `answer_present_rate=0.8`
  - `valid_parse_rate=0.72`
  - `truncation_without_answer_rate=0.18`
  - `mean_new_tokens=33.72`
  - `p95_new_tokens=128`
- 解释边界：这说明 Orca 修复已从 1 条 smoke 外推到 50 条规模后仍能产出较多合法答案，但 `128` 档截断率仍明显过高，不能据此冻结长度或写入 canonical 主表。
- Orca `max_new_tokens_192` 的第二档 provisional summary：
  - `answer_present_rate=0.86`
  - `valid_parse_rate=0.78`
  - `truncation_without_answer_rate=0.1`
  - `mean_new_tokens=42.84`
  - `p95_new_tokens=192`
- 当前解释：`192` 档比 `128` 有改善，但 10% 的 `truncation_without_answer_rate` 仍高于当前安全口径，因此还不能据此冻结长度，必须继续看 `256` 档。
- Orca `max_new_tokens_256` 的第三档 provisional summary：
  - `answer_present_rate=0.86`
  - `valid_parse_rate=0.78`
  - `truncation_without_answer_rate=0.1`
  - `mean_new_tokens=49.24`
  - `p95_new_tokens=256`
- 关键结论：
  - Orca 的 `chatml + LlamaTokenizer` 修复**确实显著优于旧 run**，但 `128/192/256` 三档都没有把 `truncation_without_answer_rate` 压到当前 2% 安全口径
  - `256` 档里仍有 `5/50` 条样本吃满 256 token 且没有 `Answer:`，例如 `MSI/2006/page_61.pdf-1`、`CAT/2017/page_85.pdf-3`、`GIS/2012/page_64.pdf-1`
  - 这些样本的 `prediction_text` 前缀都是“先分步骤解释”，说明当前主要问题是 Orca 仍倾向输出长推理文本而不是直接收束到最终答案行
- 接下来优先执行不需要大量流量的本地任务：
  - 先分析 Orca `max_new_tokens_256` 这 5 条 `truncated_suspect=True` 样本，判断是 wrapper 仍不够约束，还是必须在 provisional 层继续扩到 `320/384`
  - `LaMini-LLaMA-7B` 继续保留为待补 repo_id 占位，不下载、不 smoke
- `Xwin-LM-7B` 补下载已完成：
  - 默认 xet 路径下曾出现“进程存活但 `.incomplete` 字节数长期不增长”
  - 已改用 `HF_HUB_DISABLE_XET=1` 走普通 HTTP 续传
  - 当前 Xwin cache 体积约 `18G`
  - `/Users/sam/.cache/huggingface/hub/models--Xwin-LM--Xwin-LM-7B-V0.1` 下已无 `.incomplete/.lock`
  - `outputs/metadata/provisional/model_registry.groups-main-candidate-expansion.json` 中 `Xwin-LM-7B` 已更新为 `available_local`

## 待完成
- [[实验协议_v1]]
- [[长度校准报告]]
- [[准入筛选总结]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_主表扩展与新增模型筛查]]
- [[下一步]]

## 关键约束
- `test1147` 仅用于协议冻结后的最终一次性正式评估。
- 今晚至少交付四样结果：原始 split、`protocol_v1`、`calibration_report.csv`、`qualification_summary.csv`。
- `qa.answer` 与 `qa.exe_ans` 不可混用。validation 中有 `623/883` 条数值不一致，主要来自百分比缩放和四舍五入差异。
- 官方 split 含少量 `yes/no` 问题，现已在 `gold_numeric` 中统一映射为 `yes -> 1.0`、`no -> 0.0`，但 `raw_answer` 保留原样。
- 事件与解决方案统一记录在 [[异常与决策记录]]。
- 统一模型路径与来源在 [[模型注册表]] 中维护，不再手写。
- `Lion-7B` 的 1 条 smoke 已验证：在 `max_new_tokens=256` 下会出现“吃满上限但无 `Answer:`”的情况，当前协议已修正为会正确记作 `truncated_suspect`。
- `Lion-7B` smoke 还显示本机吞吐约 `2.89 tok/s`，这意味着若坚持今晚跑完 6 模型全流程，时间上不可行；已在 [[异常与决策记录]] 中登记并提出“主表优先”的解决方案。
- `filtered` 范围的 registry / calibration / qualification 输出现默认走 `provisional` 路径，不再覆盖 canonical 文件。
- canonical 旧 6 模型注册表里，Lion/Orca/Zephyr/MiniLLM/DeepSeek/OpenR1 仍均为 `available_local`。
- 本轮新增扩表的 provisional registry 中，`Xwin-LM-7B`、`DeepSeek-R1-Distill-Qwen-7B`、`OpenR1-Distill-7B` 均为 `available_local`，`LaMini-LLaMA-7B` 仍是 `pending_repo_id`。
- 当前 `calibration_report.csv` 显示：
  - `128 / 192 / 256` 三档都无法满足“全部主表模型 `truncation_without_answer_rate <= 2%`”
  - `Lion-7B` 在三档下均为 `1.0`
  - 因此当前协议下没有可冻结的安全档位
- 在新的长度决策确认前，不能进入正式主表 `val_screen200`
- 本轮 wrapper registry v2 smoke 的新结论：
  - `Zephyr-7B-beta -> plain` 是目前唯一 `Answer:=True` 且 `valid_parse=True` 的主表路径，可视为已进入“有效输入协议”
  - `Lion-7B -> vicuna`：special-token pad 对齐已修复，但仍无 `Answer:`、`truncation_suspect=True`、`repetition_collapse=True`
  - `Orca-2-7B -> chatml`：special-token pad 对齐已修复，但仍无 `Answer:`、`truncation_suspect=True`
  - `MiniLLM-Llama-7B -> completion`：special-token pad 对齐已修复，但仍无 `Answer:`，更适合暂时保留为 `base_distilled` baseline，而不是直接进入统一 instruction 主表
- 旧失败尝试已整理到 [[过程总结_输入包装修复]]，旧 v1 debug 文件已归档到 `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/`
- v3 最小修复结论：
  - `Lion-7B -> alpaca`：prompt 序列化已切到官方 Alpaca-style，但这条样本仍无 `Answer:`、`valid_parse=False`、`truncation_suspect=True`、`repetition_collapse=True`
  - `Orca-2-7B -> chatml + LlamaTokenizer`：这条样本已输出 `Answer: 1.33%`，`valid_parse=True`，`truncation_suspect=False`
  - 因此 Orca 旧失败结论现在开始更有解释价值；Lion 的“vicuna 失败”结论不能再直接引用，应以 v3 Alpaca 结果为准
- 当前**仍不建议立刻重跑 canonical `val_calib50`**：
  - 原因是 Lion 虽已切到 Alpaca wrapper，但仍未进入有效输入协议
  - 如果要推进，可先做 **Orca 单模型 provisional `val_calib50`**，不要直接恢复全主表 canonical calibration
- MiniLLM-Llama-7B 仍建议保持 `base_distilled` baseline 定位，不在本轮继续调 wrapper
- 当前正式 instruction 主表候选仍只有 `Orca-2-7B` 和 `Zephyr-7B-beta` 两个“已进入有效输入协议”的模型，离“不少于 4 个”还差 `2` 个。
- 当前**仍不建议恢复 canonical `val_calib50` 或启动正式主表 `val_screen200`**，因为 Orca 虽可解析率明显恢复，但 `256` 档截断率仍有 10%，新增 4 模型又暂未补出新的有效 instruction 候选。
- Xwin-LM-7B 补跑 1 条 smoke 的新结论：
  - `outputs/debug/input_smoke_expansion/Xwin-LM-7B/smoke_expansion.json` 显示 `effective_wrapper=vicuna`、`answer_appears=False`、`valid_parse=False`、`truncation_suspect=True`
  - 底层 token probe 进一步确认模型连续生成 `128` 个 token id `0`，而该 tokenizer 中 `0 == <unk> == pad_token_id`
  - 因此 `decode(skip_special_tokens=True)` 后得到空串，Xwin 当前失败形态应记为“`<unk>` 重复退化”，仍未进入有效输入协议
- Orca `256` 档 5 条截断样本复查结论：
  - 5 条样本分别是 `MSI/2006/page_61.pdf-1`、`CAT/2017/page_85.pdf-3`、`GIS/2012/page_64.pdf-1`、`MRO/2006/page_33.pdf-2`、`ILMN/2007/page_78.pdf-2`
  - 5/5 都是 `prompt_wrapper_family=chatml`、`prompt_render_mode_effective=chatml`、`effective_tokenizer_class_name=LlamaTokenizer`
  - 5/5 的 `prediction_text` 都以 `To calculate...` / `To answer this question...` 开头，先进入自然语言分步推理，吃满 `256` token 后仍没有 `Answer:` 行，因此 `parse_error_reason=missing_answer_line`
  - 非截断样本里 `45` 条中有 `40` 条直接以 `Answer:` 开头，说明 Orca 剩余 5 条失败更像“模型在个别样本上违背 direct-answer 指令，转成长推理模式”，而不是一个统一的 ChatML wrapper / tokenizer 损坏问题
  - 截断组 `prompt_length_tokens` 的 `min/mean/max=958/1416.0/1633`，非截断组是 `543/1129.1/1736`，非截断组最大 prompt 反而更长，因此这 5 条不是单纯“输入太长把输出空间挤没了”
- DeepSeek / OpenR1 下一步最小动作已经收束为“先查官方 wrapper”，不继续在当前失败路径上重复 smoke：
  - `DeepSeek-R1-Distill-Qwen-7B` 当前更像“长推理不收束 / 无 Answer 行”
  - `OpenR1-Distill-7B` 当前更像“plain wrapper 不合适 + 回显 `Answer: <numeric value>` 占位符”
- DeepSeek / OpenR1 官方 wrapper 初查新结论：
  - `DeepSeek-R1-Distill-Qwen-7B` 本地 `tokenizer_config.json` 的 `chat_template` 会在 assistant 边界后直接追加 `<｜Assistant｜><think>\\n`；[官方模型卡](https://huggingface.co/deepseek-ai/DeepSeek-R1-Distill-Qwen-7B) 又明确建议不要加 system prompt、数学题答案放 `\\boxed{}`、并强制以 `<think>\\n` 开头，所以它的官方推荐用法和当前“无 CoT + 只吃 `Answer:` 行”的主协议存在结构性冲突。
  - `OpenR1-Distill-7B` 本地 `tokenizer_config.json` 没有 `chat_template`，但保留 `<|im_start|>/<|im_end|>` special tokens；[官方模型卡](https://huggingface.co/open-r1/OpenR1-Distill-7B) 推荐 `transformers chat` 或 `pipeline(messages)` 的 user-message 入口，没有示范手写 plain 拼接，因此当前 plain 回显占位符大概率不是 parser 问题，而是序列化方式不够官方。

## 当前明确不再重复做什么
- 不再重跑 `Xwin-LM-7B` 当前 Vicuna 路径下同样的 1 条 smoke。
- 不再继续把 `Lion-7B` 拉回正式主表。
- 不再继续调 `MiniLLM-Llama-7B` wrapper。
- 不恢复 canonical `val_calib50`。
- 不恢复 canonical `val_screen200`。
- 不改 parser。
- 不把 benchmark 主协议改成 CoT。

## 相关入口
- [[项目总览]]
- [[任务笔记 - FinQA 基准重建]]
- [[会话日志]]
- [[下一步]]
- [[异常与决策记录]]
- [[模型注册表]]
- [[组员运行说明]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_协议可用性筛查]]
- [[过程总结_orca_下一步决策]]
- [[过程总结_新增候选失败类型分类]]
