# 会话日志

## 日志索引
- [[2026-04-02_会话日志]]
- [[2026-04-03_会话日志]]
- [[2026-04-04_会话日志]]
- [[2026-04-07_会话日志]]

## 使用说明
- 记录每轮目标、读取文件、执行命令、修改文件、输出结果、问题、结论和下一步。
- 详细过程以日期日志为主，本页只保留索引与总入口。

## 最新阻塞
- 2026-04-02 05:07 HKT：主表 benchmark 曾因 Orca-2-7B tokenizer 环境冲突而暂停，现已修复，详见 [[异常与决策记录]] 与 [[当前状态]]。

## 最新进展
- 2026-04-02 05:22 HKT：Orca 1 条 smoke 已通过，canonical 主表 `val_calib50` 已重新启动。
- 2026-04-02 05:40 HKT：已切换为串行 driver，完成 `calibration -> freeze -> screen-only` 链路自动化。
- 2026-04-02 11:03 HKT：canonical `calibration_report.csv` 已生成，但当前冻结规则失败，screening 暂停。
- 2026-04-04：当前最高优先级已切换为“输入接口正确性修复与验证”，新增 [[输入接口诊断]]，4 模型 1 条 smoke 已完成；目前结论是不建议立刻重跑 `val_calib50`。
- 2026-04-04：新增 wrapper registry v2 和 [[过程总结_输入包装修复]]，旧 `apply_chat_template` 失败尝试已归档到 `outputs/debug/archive/debug_failed_attempts/apply_chat_template_only_v1/`。
- 2026-04-04：完成 v3 最小修复 smoke。Lion 切到官方 Alpaca wrapper 后仍失败；Orca 切到 `chatml + LlamaTokenizer` 后，1 条样本恢复 `Answer: 1.33%` 且 `valid_parse=True`。旧 v3a 偏差输出已归档。
- 2026-04-04：启动 [[过程总结_主表扩展与新增模型筛查]]；因热点网络暂停 Xwin 下载，改为先用 `--local-only` 生成 provisional registry，并继续本地 DeepSeek/OpenR1 诊断与 Orca 单模型 provisional calibration。
- 2026-04-04：完成新增 4 模型本地 expansion smoke。Xwin/LaMini 因资源/仓库原因跳过，DeepSeek/OpenR1 暂未进入有效输入协议，因此当前正式 instruction 主表候选仍只有 Orca 与 Zephyr 两个。
- 2026-04-04：Orca 单模型 provisional `val_calib50` 三档已完成，`128/192/256` 的 `truncation_without_answer_rate` 分别为 `0.18/0.1/0.1`，因此当前仍不建议恢复 canonical 主线。
- 2026-04-04：Xwin-LM-7B 已补下载完成并刷新为 `available_local`；项目文件复查通过，下一步应先补 Xwin 1 条 debug smoke，再分析 Orca 5 条截断样本。
- 2026-04-04：Xwin-LM-7B 的补跑 smoke 已完成，但 Vicuna wrapper 下连续生成 128 个 `<unk>`，`Answer=False / valid_parse=False`，仍未进入有效输入协议；Orca 5 条 `256` 截断样本的直接原因是模型先输出长推理文本，最终没落到 `Answer:` 行。
- 2026-04-04：新增 [[过程总结_协议可用性筛查]]、[[过程总结_orca_下一步决策]]、[[过程总结_新增候选失败类型分类]] 和候选池总表 `outputs/debug/protocol_screening_registry/current_candidate_status_table.{json,md}`；当前 8 个模型里只有 Orca 与 Zephyr 进入有效输入协议，DeepSeek/OpenR1 下一步都收束为“先查官方 wrapper”。
- 2026-04-04：网络中断后复查无残留进程，已继续完成 DeepSeek / OpenR1 官方 wrapper 初查；DeepSeek 官方模板会显式推 `<think>` + `\\boxed{}`，OpenR1 官方入口更接近 `pipeline(messages)` 而非手写 plain 拼接，详见 [[过程总结_新增候选失败类型分类]]。
- 2026-04-07：已启动 [[过程总结_主流Instruct预筛]]，准备对 `Mistral/Qwen2.5/Llama-3.1/Gemma/Yi/ChatGLM3` 做 1 条 smoke 预筛，并将官方下载失败、门控失败、入口不兼容和协议失败分开记录。
- 2026-04-07：`Qwen2.5-7B-Instruct` 已完成单模型下载与 1 条 smoke，结果为 `Answer=True / valid_parse=True / truncation_suspect=False`；它是本轮新增主流模型里第一个通过 smoke 的候选。
- 2026-04-07：`Yi-1.5-6B-Chat` 已完成单模型下载与 1 条 smoke，结果为 `Answer=True / valid_parse=True / truncation_suspect=False`；当前已进入有效输入协议的模型增至 `4` 个。
- 2026-04-07：`ChatGLM3-6B` 已完成单模型下载，但首条 smoke 不是协议 pass/fail，而是在加载阶段报 `AttributeError: 'ChatGLMConfig' object has no attribute 'max_length'`；当前应先修加载入口兼容问题。
- 2026-04-07：`ChatGLM3-6B` 的入口兼容修复已完成；在保留官方 `build_chat_input + prepare_inputs_for_generation + process_response` 的前提下，改为本地手动更新 `past_key_values / attention_mask / position_ids`，重跑后输出 `Answer: 17.8`，smoke 通过，已进入有效输入协议。
- 2026-04-07：已启动 6 模型 fresh `val_calib50` calibration，统一测 `128 / 192 / 256` 三档，输出只写入 `outputs/provisional/calibration_runs_6model_20260407/` 与 `outputs/provisional/calibration_report.6model_mainstream_20260407.csv`。
- 2026-04-07：当前 fresh calibration 已完成 `Orca-2-7B` 三档，`truncation_without_answer_rate=0.18 / 0.10 / 0.10`；现已切到 `Zephyr-7B-beta / 128`，早期样本已出现截断，说明这轮 calibration 不能仅凭 smoke 结果替代。
- 2026-04-07：已新增“模型 × 档位”过程表，位置为 `outputs/provisional/calibration_table.6model_mainstream_20260407.{md,json}`；已完成档位写正式值，进行中档位写中间统计，便于实时查看。
- 2026-04-07：当前 fresh calibration 已完成前 2 个模型共 6 个档位：`Orca-2-7B` 为 `0.18 / 0.10 / 0.10`，`Zephyr-7B-beta` 为 `0.32 / 0.14 / 0.06`；现已切到 `Mistral-7B-Instruct-v0.3 / 128`。
- 2026-04-07：当前 fresh calibration 已继续完成 `Qwen2.5-7B-Instruct` 三档 `0.00 / 0.00 / 0.00`、`Mistral-7B-Instruct-v0.3` 三档 `0.00 / 0.00 / 0.00`，以及 `Yi-1.5-6B-Chat` 的 `128 / 192 = 0.02 / 0.02`；现已切到 `Yi-1.5-6B-Chat / 256`。
- 2026-04-07：fresh calibration 已形式上跑完整个 6 模型集合，并生成 `calibration_report.6model_mainstream_20260407.csv`；但发现 `ChatGLM3-6B / 128` 的 `summary/report` 与 `predictions.jsonl` 行数不一致，因此当前 aggregate csv 只能记为 `provisional_suspect`。
- 2026-04-07：`ChatGLM3-6B / 128` 已单独重跑修复；旧 44 行版本已归档，新版本 `predictions=50` 且 `summary.num_examples=50`。全量一致性自检通过，aggregate csv 已重建完成。
- 2026-04-07：已冻结 6 模型 `val_screen200` 规则，并初始化 `outputs/provisional/screen200_6model_20260407/` 及 `screen200_status_table.{md,json}`；接下来按 `Orca -> Zephyr -> Qwen2.5 -> Mistral -> Yi -> ChatGLM3` 逐模型运行。
- 2026-04-07：已启动 `Orca-2-7B / val_screen200 / 256`；`screen200_status_table` 已增强为在无 `summary.json` 时显示 `running_no_summary` 和当前 `predictions_lines`。
- 2026-04-07：执行策略改为“增量落盘”：每个 `screen200` 模型在启动、中间写入样本、完成刷新总表三个节点都先写 markdown，再继续下一模型，避免中断后无日志证据。
- 2026-04-07：用户要求“`Orca` 跑完先停，只跑一个”；因此 `Zephyr-7B-beta` 的半截 `screen200` 运行已停止并归档，当前活跃汇总表仅保留 `Orca-2-7B` 的正式结果。
- 2026-04-07：用户随后确认继续。恢复前检查发现活跃目录里 `Zephyr-7B-beta` 已累计到 `89/200` 行，但外层 `run_qualification_v1.py` 已不存在且 `summary.json` 未生成，因此这份产物目前只能当中间证据，不能直接算正式结果。
- 2026-04-07：进一步排查确认 `Zephyr-7B-beta` 的内层 `run_finqa_local_benchmark_v1.py` 仍在继续运行；当前不重开新命令，改为直接监控这个现有进程并继续增量落盘。
- 2026-04-07：`Zephyr-7B-beta` 已生成 `summary.json`，但再次出现“`summary.num_examples` 与 `predictions.jsonl` 行数不一致”的结果一致性异常；当前只能记为 `provisional_suspect`，已暂停继续下一个模型。
- 2026-04-07：`Zephyr-7B-beta` 的最小修复已完成；重跑后 `predictions.jsonl=200` 且 `summary.num_examples=200`，一致性恢复，但模型指标仍未通过冻结门槛，因此当前正式状态为 `ok / no`。
- 2026-04-07：已继续启动 `Mistral-7B-Instruct-v0.3 / val_screen200 / 256`；当前外层进程正常，等待第一批逐样本输出落盘。
- 2026-04-07：`Mistral-7B-Instruct-v0.3` 已完成并通过冻结门槛，当前正式状态为 `ok / yes`。
- 2026-04-07：已继续启动 `Qwen2.5-7B-Instruct / val_screen200 / 256`；当前外层进程正常，等待第一批逐样本输出落盘。
- 2026-04-07：`Qwen2.5-7B-Instruct` 已完成并通过冻结门槛，当前正式状态为 `ok / yes`。
- 2026-04-07：已切换到下一个模型 `Yi-1.5-6B-Chat / val_screen200 / 256`，继续按“启动后先写文档、落第一批样本再补一次”的增量方式执行。
- 2026-04-07：`Yi-1.5-6B-Chat` 的第一批样本已开始落盘，当前总表状态为 `running_no_summary / predictions_lines=4`。
- 2026-04-07：`Yi-1.5-6B-Chat` 已完成并通过冻结门槛，当前正式状态为 `ok / yes`。
- 2026-04-07：已启动 `ChatGLM3-6B / val_screen200 / 256`；当前外层 `run_qualification_v1.py` 和内层 `run_finqa_local_benchmark_v1.py` 都正常运行，`predictions.jsonl` 已写到 `17` 行，`summary.json` 尚未生成。
- 2026-04-07：`ChatGLM3-6B` 已完成；`predictions.jsonl=200` 且 `summary.json.num_examples=200`，一致性正常。正式指标为 `runtime_success=1.000 / format_ok=0.830 / valid_parse=0.595 / truncation=0.045 / EM=0.020 / TM=0.040`，当前正式状态记为 `ok / no`。
- 2026-04-07：本轮 `6` 模型 `val_screen200` 已全部完成；当前通过冻结门槛的模型为 `Orca-2-7B`、`Mistral-7B-Instruct-v0.3`、`Qwen2.5-7B-Instruct`、`Yi-1.5-6B-Chat`，未通过的模型为 `Zephyr-7B-beta`、`ChatGLM3-6B`。
- 2026-04-07：已将本轮 `screen200` 结果整理成正式汇总文件 `screen200_final_summary.{md,json}`，并同步更新 `[[准入筛选总结]]`，当前可直接引用 4 个通过模型和 2 个未通过模型的最终结论。
- 2026-04-07：已完成“评分与报告整理”任务：新增 `scripts/relaxed_scoring.py` 作为 strict 主线的补充评分模块，新增 `scripts/run_validation883_assigned_v1.py` 作为组员单模型 validation883 wrapper，并补齐 `[[模型谱系与主表角色说明]]`、`[[评分逻辑与报告写作指南]]`、`[[组员运行说明_validation883]]`、`[[当前主表建议]]` 与 `requirements-validation883.txt`。
- 2026-04-07：静态检查已完成：新增/修改 python 文件 `py_compile` 通过；`relaxed_scoring` 在 `Lion` 环境中 import 成功；当前 `docs/ + logs/` 的 wikilinks 扫描未见明显断链。
- 2026-04-07：已修正组员默认运行入口：旧 `[[组员运行说明]]` 中“默认只跑 screening”已降级为历史说明；`scripts/run_assigned_model_v1.sh` 现在默认跑 `validation883`，只有显式加 `--legacy-screen-only` 时才回退到旧 `screen200` 流程。
- 2026-04-07：用户决定把 `ChatGLM3-6B` 以 `boundary-pass candidate` 身份并入 `validation883` 执行名单；因此当前 `validation883` 名单改为 `5` 个模型，而不是 `4` 个正式通过模型。
- 2026-04-07：已启动 `Qwen2.5-7B-Instruct` 的 `validation883`，作为今晚先跑的第一个模型。
- 2026-04-07：`Qwen2.5-7B-Instruct` 的 `validation883` 当前还在模型初始化阶段；外层 wrapper 与内层 benchmark 都已启动，输出目录已创建，但 `predictions.jsonl` 尚未开始落盘。
- 2026-04-07：`Qwen2.5-7B-Instruct` 的 `validation883` 已进入逐样本落盘阶段，当前 `predictions.jsonl = 9`，`summary.json` 尚未生成。
- 2026-04-08：`Qwen2.5-7B-Instruct` 的 `validation883` 仍在正常推进，当前 `predictions.jsonl ≈ 202/883`；按最近 `3` 条 / `10s` 的活跃速度估计，若机器保持唤醒，剩余大约还需 `45 - 70` 分钟。跨夜 `etime` 不能直接当作剩余时间估计依据。
- 2026-04-08：`Qwen2.5-7B-Instruct` 已进入尾段，当前 `predictions.jsonl ≈ 770/883`；最近 `5` 秒约新增 `2` 条，若机器保持唤醒，剩余大约还需 `5 - 10` 分钟。
- 2026-04-08：`Qwen2.5-7B-Instruct` 的 `validation883` 已完成；`predictions.jsonl=883` 且 `summary.json.num_examples=883`，一致性正常。正式指标为 `Strict TM=0.359003 / Strict EM=0.225368 / Relaxed TM=0.360136 / format_ok=0.997735 / valid_parse=0.981880 / truncation=0.000000`。
- 2026-04-08：已建立 `validation883` 汇总表 `validation883_status_table.{md,json}`；当前 `Qwen2.5-7B-Instruct = ok`，`Mistral-7B-Instruct-v0.3 = running_no_summary / predictions_lines=9`。
- 2026-04-08：`Mistral-7B-Instruct-v0.3` 已进入尾段，当前 `predictions.jsonl ≈ 778/883`；最近 `5` 秒约新增 `1` 条，若机器保持唤醒，剩余大约还需 `8 - 15` 分钟。
- 2026-04-08：`Mistral-7B-Instruct-v0.3` 的 `validation883` 已完成；`predictions.jsonl=883` 且 `summary.json.num_examples=883`，一致性正常。正式指标为 `Strict TM=0.166478 / Strict EM=0.093998 / Relaxed TM=0.194790 / format_ok=0.998867 / valid_parse=0.778029 / truncation=0.000000`。
- 2026-04-08：已启动 `Orca-2-7B / validation883`；当前 wrapper 与内层 benchmark 都正常，输出目录已创建，但 `predictions.jsonl` 尚未开始落盘，因此当前仍在初始化阶段。
- 2026-04-08：`Orca-2-7B` 的 `validation883` 已完成；`predictions.jsonl=883` 且 `summary.json.num_examples=883`，一致性正常。正式指标为 `Strict TM=0.065685 / Strict EM=0.036240 / Relaxed TM=0.080408 / format_ok=0.950170 / valid_parse=0.838052 / truncation=0.026048`。
- 2026-04-08：已确认 `Orca-2-7B` 结束后当前没有残留 `validation883` 进程；`Yi-1.5-6B-Chat` 与 `ChatGLM3-6B` 尚未启动，下一步改为继续接续 `Yi-1.5-6B-Chat / validation883`。
- 2026-04-08：`Yi-1.5-6B-Chat / validation883` 已启动；当前外层 wrapper 与内层 benchmark 都正常，`outputs/provisional/validation883_assigned/Yi-1.5-6B-Chat/` 已创建，`predictions.jsonl` 已开始落盘，当前约 `10 / 883`。
- 2026-04-08：已按用户要求重新读取 `Orca-2-7B/summary.json` 并补记结果；当前 `Orca` 结果确认无丢失，`Yi-1.5-6B-Chat` 仍在运行，统一进度表已刷新到 `running_no_summary`。
- 2026-04-08：`Yi-1.5-6B-Chat` 的 `validation883` 已完成；`predictions.jsonl=883` 且 `summary.json.num_examples=883`，一致性正常。正式指标为 `Strict TM=0.099660 / Strict EM=0.054360 / Relaxed TM=0.152888 / format_ok=0.898075 / valid_parse=0.742922 / truncation=0.005663`。
- 2026-04-08：已确认 `Yi` 完成后当前没有残留 `validation883` 进程；下一步切换到 `ChatGLM3-6B / validation883`，并沿用已验证过的 ChatGLM3 generation 兼容修补路径。
- 2026-04-08：`ChatGLM3-6B / validation883` 已启动；当前外层 wrapper 与内层 benchmark 都正常，`outputs/provisional/validation883_assigned/ChatGLM3-6B/` 已创建，`predictions.jsonl` 已开始落盘，当前约 `9 / 883`。

## 关联文档
- [[当前状态]]
- [[任务笔记 - FinQA 基准重建]]
- [[下一步]]
- [[异常与决策记录]]
- [[输入接口诊断]]
- [[过程总结_输入包装修复]]
- [[过程总结_主表扩展与新增模型筛查]]
- [[过程总结_协议可用性筛查]]
- [[过程总结_orca_下一步决策]]
- [[过程总结_新增候选失败类型分类]]
- [[过程总结_主流Instruct预筛]]
- [[组员运行说明]]
