# 过程总结_新增候选失败类型分类

## 文档目的
- 只基于当前已有 smoke/diagnosis 结果，对 `DeepSeek-R1-Distill-Qwen-7B` 和 `OpenR1-Distill-7B` 给出“失败类型判断 + 一个最小下一步动作”。
- 当前不继续乱试 wrapper，不改 parser，不引入 CoT，不恢复 canonical 主线。
- 关联文档：[[过程总结_协议可用性筛查]]、[[过程总结_主表扩展与新增模型筛查]]、[[当前状态]]、[[下一步]]、[[异常与决策记录]]

## DeepSeek-R1-Distill-Qwen-7B

### 当前证据
- `outputs/debug/input_interface_diagnosis_expansion/DeepSeek-R1-Distill-Qwen-7B.json`
  - `default_wrapper=chat_template`
  - `allow_chat_template=true`
  - `effective_tokenizer_class_name=TokenizersBackend`
  - `tokenizer.chat_template_exists=true`
  - 渲染后的 prompt 末尾带 `<｜Assistant｜><think>`
- `outputs/debug/input_smoke_expansion/DeepSeek-R1-Distill-Qwen-7B/smoke_expansion.json`
  - `answer_appears=false`
  - `valid_parse=false`
  - `truncation_suspect=true`
  - `parse_error_reason=missing_answer_line`
  - `raw_generation_text` 以 “Okay, so I need to figure out...” 开头，直接进入自然语言推理

### 失败类型判断
- 更像**长推理不收束 / 无 Answer 行**。
- 当前证据**不足以先归因到 tokenizer / generation boundary 损坏**，因为 chat_template 有 assistant 边界，但模型仍主动进入 `<think>` 风格推理。

### 下一步最小动作
- **查官方 wrapper**
- 具体含义：
  - 优先核对 DeepSeek 官方是否有推荐的非 `<think>` 入口、特殊 system/user/assistant 包装方式，或是否本来就默认要求 reasoning trace。
  - 在确认官方 wrapper 前，不继续盲试 parser，不先加 CoT 后处理，也不直接把该模型算作有效候选。

### 官方 wrapper 初查结论（2026-04-04 HKT）
- 本地 snapshot 没有 `README.md`，但 `tokenizer_config.json` 里内置了 `chat_template`。
- 这个 `chat_template` 在 `add_generation_prompt=True` 时会直接追加 `'<｜Assistant｜><think>\\n'`，这和当前 smoke 里看到“先进入自然语言/think 风格长推理”是吻合的。
- [DeepSeek 官方模型卡](https://huggingface.co/deepseek-ai/DeepSeek-R1-Distill-Qwen-7B) 的 Usage Recommendations 明确写了三点对当前协议很关键的要求：
  - 不建议加 system prompt，所有指令应放在 user prompt
  - 数学题建议要求模型 step by step 并把最终答案放在 `\\boxed{}`
  - 为了强制 reasoning，建议每个输出都以 `<think>\\n` 开头
- 这意味着：**DeepSeek 当前官方推荐用法本身就和我们“不引入 CoT + parser 只吃最终 `Answer:` 行”的主协议有结构性冲突**。
- 当前收束判断：
  - DeepSeek 的失败不是一个简单“chat_template 没生效”的问题，反而是“官方 chat_template/官方建议正在把模型推向 `<think>` + `\\boxed{}` 风格输出”
  - 因此下一步如果还要救 DeepSeek，应该先做“是否允许对 DeepSeek 采用 user-only 且无 `<think>` 前缀的 Qwen-style serializer”这一项**provisional 协议消融设计**，而不是马上再跑同一路径 smoke

## OpenR1-Distill-7B

### 当前证据
- `outputs/debug/input_interface_diagnosis_expansion/OpenR1-Distill-7B.json`
  - `default_wrapper=chatml`
  - `allow_chat_template=false`
  - `effective_tokenizer_class_name=Qwen2Tokenizer`
  - `tokenizer.chat_template_exists=false`
  - ChatML prompt 有 `<|im_start|>assistant` 边界；plain prompt 没有 assistant 边界
- `outputs/debug/input_smoke_expansion/OpenR1-Distill-7B/smoke_expansion.json`
  - `chatml` candidate：输出进入 `\\boxed{}` / `</think>` + 长推理风格，`answer_appears=false`，`truncation_suspect=true`
  - `plain` candidate：输出包含 `Answer:`，但 `answer_line_raw='Answer: <numeric value>'`，只是回显占位符，`valid_parse=false`，`multiple_answer_lines=true`

### 失败类型判断
- 更像**plain wrapper 不合适 + 回显占位符**，并且 ChatML 路径也偏向 reasoning-style completion。
- 当前问题**不是 parser 不兼容**，因为 plain 路径最后一行本身就是占位符文本，不是合法数值。

### 下一步最小动作
- **查官方 wrapper**
- 具体含义：
  - 优先确认 OpenR1 官方推荐的对话/指令序列化方式和 generation boundary，而不是继续在 `plain/chatml` 两个自定义包装之间盲试。
  - 在官方 wrapper 没查清前，暂不改 tokenizer policy，不改 parser，也不把 OpenR1 算作已进入有效输入协议。

### 官方 wrapper 初查结论（2026-04-04 HKT）
- 本地 snapshot 没有 `README.md`，且 `tokenizer_config.json` 里**没有** `chat_template` 字段。
- 但本地 tokenizer 仍定义了 `<|im_start|>` / `<|im_end|>` special tokens；`generation_config.json` 的 `eos_token_id=151645` 对应 `<|im_end|>`，而原始 `config.json` 的 `eos_token_id=151643` 对应 `<|endoftext|>`，这也解释了为什么我们前面需要做 pad/eos 对齐。
- [OpenR1 官方模型卡](https://huggingface.co/open-r1/OpenR1-Distill-7B) 推荐的入口是：
  - `transformers chat open-r1/OpenR1-Distill-7B ...`
  - 或 `pipeline("text-generation", model="open-r1/OpenR1-Distill-7B", ...); outputs = pipe(messages, ...)`
- 官方示例只给了 `messages = [{"role": "user", "content": "..."}]`，没有展示 system prompt，也没有展示手写 plain 文本拼接。
- 这意味着：**OpenR1 当前 plain 路径下回显 `Answer: <numeric value>`，很可能就是“非官方序列化方式 + 模型偏 reasoning 数据格式”的组合结果**，而不是 parser 自身问题。
- 当前收束判断：
  - 下一步不应该继续 blind test `plain` vs `chatml`
  - 更合理的最小动作是先查 `transformers chat` / `pipeline(messages)` 对这个 tokenizer 实际会使用什么模板；如果模板可还原，再只在 `outputs/debug/` 下做一条“官方 messages 序列化”对照 smoke

## 暂不重复动作
- 不再重复跑 DeepSeek 当前 `chat_template` 路径下同一条 smoke。
- 不再重复跑 OpenR1 当前 `plain/chatml` 两条路径下同一条 smoke。
- 不改 parser。
- 不把主协议改成 CoT。
